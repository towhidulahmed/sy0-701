export type LinuxTopic = {
  slug: string;
  title: string;
  content: string;
  commands: { cmd: string; desc: string }[];
  tips: string[];
  flashcards: { front: string; back: string }[];
};

export type LinuxLevel = "beginner" | "intermediate" | "advanced";

export type LinuxDomain = {
  key: string;
  name: string;
  level: LinuxLevel;
  topics: LinuxTopic[];
};

export const LINUX_STUDY: LinuxDomain[] = [
  {
    key: "fundamentals",
    name: "Linux Fundamentals",
    level: "beginner",
    topics: [
      {
        slug: "basic-navigation",
        title: "Basic Navigation",
        content: `The Linux filesystem is a hierarchical tree starting from the root directory (/). Understanding how to navigate this tree is the most fundamental skill.\n\n**pwd (Print Working Directory)** shows your current location in the filesystem. When you first open a terminal, you typically start in your home directory (~), which is usually /home/username.\n\n**ls (List)** displays the contents of a directory. Without arguments it lists the current directory. Key options:\n- \`ls -l\` — long format showing permissions, owner, group, size, and modification date\n- \`ls -a\` — show hidden files (those starting with .)\n- \`ls -h\` — human-readable file sizes (KB, MB, GB)\n- \`ls -R\` — recursive listing of subdirectories\n- \`ls -lt\` — sort by modification time\n\n**cd (Change Directory)** moves you to a different directory.\n- \`cd /etc\` — absolute path (starts from root)\n- \`cd Documents\` — relative path (from current location)\n- \`cd ~\` or \`cd\` — go to home directory\n- \`cd ..\` — go up one level\n- \`cd -\` — go to previous directory\n\n**Path Types:**\n- **Absolute path**: starts from / (root), e.g., /var/log/syslog\n- **Relative path**: starts from current directory, e.g., ../config/settings\n\n**Special path references:**\n- \`~\` — home directory\n- \`.\` — current directory\n- \`..\` — parent directory\n\n**TAB completion** is essential for efficiency. Start typing a path and press TAB to auto-complete. Press TAB twice to see all matching options.`,
        commands: [
          { cmd: "pwd", desc: "Print current working directory" },
          { cmd: "ls -lah", desc: "List all files in long format with human-readable sizes" },
          { cmd: "cd /var/log", desc: "Change to /var/log directory" },
          { cmd: "cd ~", desc: "Change to home directory" },
          { cmd: "cd ..", desc: "Go up one directory level" },
          { cmd: "cd -", desc: "Return to previous directory" },
        ],
        tips: [
          "Use TAB completion constantly — it saves time and prevents typos",
          "Use 'cd -' to toggle between two directories quickly",
          "Combine ls flags: 'ls -lah' is the most common combination",
          "Use 'ls -lt | head' to see the most recently modified files",
        ],
        flashcards: [
          { front: "What does pwd stand for and do?", back: "Print Working Directory — displays your current location in the filesystem" },
          { front: "How do you list hidden files?", back: "ls -a (hidden files start with a dot)" },
          { front: "What is the difference between an absolute and relative path?", back: "Absolute starts from root (/), relative starts from current directory" },
          { front: "What does ~ represent in a path?", back: "The current user's home directory (/home/username)" },
          { front: "What does 'cd -' do?", back: "Changes to the previous working directory" },
          { front: "What does '..' represent?", back: "The parent directory (one level up)" },
        ],
      },
      {
        slug: "file-manipulation",
        title: "File Manipulation",
        content: `Linux provides powerful commands for creating, copying, moving, and deleting files and directories.\n\n**mkdir (Make Directory)** creates new directories.\n- \`mkdir photos\` — create a single directory\n- \`mkdir -p path/to/deep/dir\` — create parent directories as needed\n- \`mkdir -m 755 mydir\` — create with specific permissions\n\n**rmdir** removes empty directories only. For non-empty directories, use \`rm -r\`.\n\n**touch** creates empty files or updates timestamps of existing files.\n- \`touch newfile.txt\` — create an empty file\n- \`touch -t 202301011200 file\` — set specific timestamp\n\n**cp (Copy)** copies files and directories.\n- \`cp source.txt dest.txt\` — copy a file\n- \`cp -r sourcedir/ destdir/\` — copy directory recursively\n- \`cp -p file1 file2\` — preserve permissions, ownership, timestamps\n- \`cp -i file1 file2\` — interactive (prompt before overwrite)\n\n**mv (Move/Rename)** moves or renames files and directories.\n- \`mv old.txt new.txt\` — rename a file\n- \`mv file.txt /tmp/\` — move to another directory\n- \`mv -i source dest\` — prompt before overwrite\n\n**rm (Remove)** deletes files and directories.\n- \`rm file.txt\` — remove a file\n- \`rm -r directory/\` — remove directory and contents recursively\n- \`rm -f file.txt\` — force remove without prompting\n- \`rm -rf directory/\` — force remove directory (use with extreme caution!)\n\n**Wildcards** can be used with any command:\n- \`*\` — zero or more characters: \`*.txt\` matches all .txt files\n- \`?\` — exactly one character: \`file?.txt\` matches file1.txt, fileA.txt\n- \`[]\` — character range: \`file[1-3].txt\` matches file1.txt, file2.txt, file3.txt`,
        commands: [
          { cmd: "mkdir -p project/src/lib", desc: "Create nested directory structure" },
          { cmd: "touch index.html style.css", desc: "Create multiple empty files" },
          { cmd: "cp -r src/ backup/", desc: "Copy directory recursively" },
          { cmd: "mv *.log /var/log/", desc: "Move all .log files" },
          { cmd: "rm -rf /tmp/old-cache/", desc: "Force remove directory" },
          { cmd: "rmdir emptydir", desc: "Remove an empty directory" },
        ],
        tips: [
          "Always use 'rm -i' or alias rm='rm -i' to prevent accidental deletion",
          "Never run 'rm -rf /' — it will destroy your entire system",
          "Use 'cp -a' to archive (preserve everything including symlinks)",
          "mkdir -p is safe to run even if the directory already exists",
        ],
        flashcards: [
          { front: "How do you create nested directories in one command?", back: "mkdir -p path/to/nested/dir" },
          { front: "What does 'rm -rf' do?", back: "Force removes files/directories recursively without prompting" },
          { front: "How do you copy a directory and all its contents?", back: "cp -r sourcedir/ destdir/" },
          { front: "What wildcard matches exactly one character?", back: "? (question mark)" },
          { front: "What does 'touch' do if the file already exists?", back: "Updates the file's access and modification timestamps" },
          { front: "How do you rename a file in Linux?", back: "mv oldname newname" },
        ],
      },
      {
        slug: "file-types-info",
        title: "Files & File Types",
        content: `In Linux, everything is a file — regular files, directories, devices, and even processes. Understanding file types is essential.\n\n**file command** determines the type of a file by examining its content, not its extension.\n- \`file document.pdf\` — shows "PDF document"\n- \`file /bin/ls\` — shows "ELF 64-bit LSB executable"\n\n**File Types in ls -l** (first character of permissions):\n- \`-\` — regular file\n- \`d\` — directory\n- \`l\` — symbolic link\n- \`c\` — character device (e.g., /dev/tty)\n- \`b\` — block device (e.g., /dev/sda)\n- \`p\` — named pipe (FIFO)\n- \`s\` — socket\n\n**Hidden files** start with a dot (.) — e.g., .bashrc, .ssh/. They are hidden from normal \`ls\` output. Use \`ls -a\` to see them.\n\n**Spaces in filenames** can be handled two ways:\n- Quotes: \`cd "My Documents"\`\n- Backslash: \`cd My\\ Documents\`\n\n**stat** command shows detailed file information including inode, size, blocks, access/modify/change times.\n\n**Key directories:**\n- /bin — essential user binaries\n- /sbin — system binaries\n- /etc — configuration files\n- /home — user home directories\n- /var — variable data (logs, caches)\n- /tmp — temporary files\n- /usr — user programs and data\n- /dev — device files\n- /proc — process information (virtual filesystem)\n- /opt — optional/third-party software`,
        commands: [
          { cmd: "file myfile.dat", desc: "Determine file type" },
          { cmd: "stat filename", desc: "Display detailed file information" },
          { cmd: "ls -la", desc: "List all files including hidden" },
          { cmd: "which command", desc: "Show full path of a command" },
          { cmd: "whereis binary", desc: "Locate binary, source, and man page" },
          { cmd: "type command", desc: "Show how a command would be interpreted" },
        ],
        tips: [
          "Linux does not rely on file extensions — 'file' command checks actual content",
          "The /proc filesystem is virtual — it provides info about running processes",
          "Use 'ls -F' to append indicators (/ for dirs, * for executables, @ for links)",
          "Everything in /dev represents a device or pseudo-device",
        ],
        flashcards: [
          { front: "What does the 'file' command do?", back: "Determines the type of a file by examining its content, not extension" },
          { front: "What does 'd' mean as the first character in ls -l output?", back: "The entry is a directory" },
          { front: "Where are system configuration files typically stored?", back: "/etc directory" },
          { front: "How do you handle filenames with spaces?", back: "Use quotes (\"my file\") or backslash escape (my\\ file)" },
          { front: "What is /proc?", back: "A virtual filesystem providing information about running processes and system info" },
          { front: "Where are log files typically stored?", back: "/var/log" },
        ],
      },
    ],
  },
  {
    key: "permissions-ownership",
    name: "Permissions & Ownership",
    level: "beginner",
    topics: [
      {
        slug: "file-permissions",
        title: "File Permissions",
        content: `Linux file permissions control who can read, write, and execute files. Every file has three permission sets: owner (user), group, and others.\n\n**Permission types:**\n- **r (read = 4)** — view file contents / list directory contents\n- **w (write = 2)** — modify file / create/delete files in directory\n- **x (execute = 1)** — run as program / access (cd into) directory\n\n**Reading permissions** from \`ls -l\`:\n\`-rwxr-xr--\` breaks down as:\n- \`-\` = regular file\n- \`rwx\` = owner can read, write, execute\n- \`r-x\` = group can read and execute\n- \`r--\` = others can only read\n\n**chmod (Change Mode)** — modify permissions:\n- **Numeric/Octal**: \`chmod 755 script.sh\` (rwxr-xr-x)\n  - 7 = 4+2+1 (rwx), 5 = 4+0+1 (r-x)\n- **Symbolic**: \`chmod u+x script.sh\` (add execute for user)\n  - u=user, g=group, o=others, a=all\n  - +=add, -=remove, ==set exactly\n  - \`chmod g+w file\` — add write for group\n  - \`chmod o-rwx file\` — remove all permissions for others\n  - \`chmod a+r file\` — add read for everyone\n- **Recursive**: \`chmod -R 755 directory/\`\n\n**Common permission patterns:**\n- 755 — directories, executable scripts (owner full, others read+execute)\n- 644 — regular files (owner read+write, others read only)\n- 700 — private directories (owner only)\n- 600 — private files like SSH keys\n- 777 — everyone full access (avoid this!)`,
        commands: [
          { cmd: "chmod 755 script.sh", desc: "Set rwxr-xr-x permissions" },
          { cmd: "chmod u+x file", desc: "Add execute permission for owner" },
          { cmd: "chmod -R 644 docs/", desc: "Set permissions recursively" },
          { cmd: "chmod go-w file", desc: "Remove write from group and others" },
          { cmd: "ls -l file", desc: "View file permissions" },
        ],
        tips: [
          "Directories need execute (x) permission to be entered with cd",
          "Scripts need execute permission to run: chmod +x script.sh",
          "Never chmod 777 in production — it's a security risk",
          "Use 'stat -c %a file' to see numeric permissions",
        ],
        flashcards: [
          { front: "What numeric value represents rwxr-xr-x?", back: "755" },
          { front: "What does the execute permission mean on a directory?", back: "Allows entering the directory with cd and accessing its contents" },
          { front: "How do you add execute permission for the file owner?", back: "chmod u+x filename" },
          { front: "What permission should SSH private keys have?", back: "600 (read/write for owner only)" },
          { front: "What do r, w, x correspond to numerically?", back: "r=4, w=2, x=1" },
          { front: "What does 'chmod -R' do?", back: "Applies permission changes recursively to all files/directories within" },
        ],
      },
      {
        slug: "ownership-umask",
        title: "Ownership & Umask",
        content: `Every file in Linux has an owner (user) and a group. Ownership determines which permission set applies to whom.\n\n**chown (Change Owner)** — change file owner and/or group:\n- \`chown alice file.txt\` — change owner to alice\n- \`chown alice:developers file.txt\` — change owner and group\n- \`chown :developers file.txt\` — change group only\n- \`chown -R alice:staff /project/\` — recursive ownership change\n\n**chgrp (Change Group)** — change group only:\n- \`chgrp developers project/\`\n\n**umask** controls the default permissions for newly created files and directories.\n- Default file permissions: 666 (rw-rw-rw-)\n- Default directory permissions: 777 (rwxrwxrwx)\n- The umask value is SUBTRACTED from these defaults\n- Common umask: 022 → files get 644, directories get 755\n- Restrictive umask: 077 → files get 600, directories get 700\n\n**Setting umask:**\n- \`umask\` — display current umask\n- \`umask 027\` — set umask for current session\n- Add to ~/.bashrc for persistence\n\n**Special permissions:**\n- **SUID (4xxx)**: file executes as file owner, not as user running it. e.g., /usr/bin/passwd\n- **SGID (2xxx)**: file executes as group owner; on directories, new files inherit the directory's group\n- **Sticky bit (1xxx)**: only file owner can delete files in directory. e.g., /tmp has sticky bit set\n\nExamples:\n- \`chmod 4755 program\` — set SUID\n- \`chmod 2755 shared/\` — set SGID on directory\n- \`chmod 1777 /tmp\` — set sticky bit`,
        commands: [
          { cmd: "chown user:group file", desc: "Change owner and group" },
          { cmd: "chown -R www-data:www-data /var/www/", desc: "Recursive ownership change" },
          { cmd: "chgrp developers project/", desc: "Change group ownership" },
          { cmd: "umask", desc: "Display current umask value" },
          { cmd: "umask 027", desc: "Set restrictive umask" },
          { cmd: "chmod 4755 program", desc: "Set SUID bit" },
        ],
        tips: [
          "Only root can change file ownership with chown",
          "SUID on scripts is a security risk — most systems ignore it",
          "The sticky bit on /tmp prevents users from deleting each other's files",
          "SGID on shared directories ensures new files belong to the directory's group",
        ],
        flashcards: [
          { front: "What does umask 022 result in for new files?", back: "644 (666 - 022 = 644, rw-r--r--)" },
          { front: "What is the SUID bit?", back: "Special permission (4xxx) that makes a file execute with the file owner's privileges" },
          { front: "What does the sticky bit do on a directory?", back: "Only the file owner (or root) can delete or rename files within it" },
          { front: "How do you change both owner and group at once?", back: "chown owner:group filename" },
          { front: "What does SGID on a directory do?", back: "New files created in the directory inherit the directory's group ownership" },
          { front: "Who can run chown?", back: "Only root (superuser) can change file ownership" },
        ],
      },
    ],
  },
  {
    key: "text-processing",
    name: "Text Processing & Filters",
    level: "beginner",
    topics: [
      {
        slug: "viewing-files",
        title: "Viewing File Contents",
        content: `Linux provides several commands for viewing file contents in different ways.\n\n**cat (Concatenate)** — display entire file contents:\n- \`cat file.txt\` — show file\n- \`cat -n file.txt\` — show with line numbers\n- \`cat file1 file2 > combined\` — concatenate files\n\n**less** — view files with pagination (better for large files):\n- Navigate: Space (page down), b (page up), / (search), q (quit)\n- \`less +F file.log\` — follow mode (like tail -f)\n\n**head** — show first lines of a file:\n- \`head file.txt\` — first 10 lines (default)\n- \`head -n 20 file.txt\` — first 20 lines\n- \`head -c 100 file.txt\` — first 100 bytes\n\n**tail** — show last lines of a file:\n- \`tail file.txt\` — last 10 lines\n- \`tail -n 50 file.txt\` — last 50 lines\n- \`tail -f /var/log/syslog\` — follow (watch for new lines in real-time)\n- \`tail -f -n 0 file\` — only show new lines\n\n**wc (Word Count)** — count lines, words, characters:\n- \`wc file.txt\` — lines, words, characters\n- \`wc -l file.txt\` — count lines only\n- \`wc -w file.txt\` — count words only\n- \`wc -c file.txt\` — count bytes\n\n**nl** — number lines: \`nl file.txt\`\n\n**tac** — display file in reverse (last line first)`,
        commands: [
          { cmd: "cat -n file.txt", desc: "Display file with line numbers" },
          { cmd: "less /var/log/syslog", desc: "View large file with pagination" },
          { cmd: "head -20 file.txt", desc: "Show first 20 lines" },
          { cmd: "tail -f /var/log/auth.log", desc: "Follow log file in real-time" },
          { cmd: "wc -l *.py", desc: "Count lines in all Python files" },
        ],
        tips: [
          "Use 'less' instead of 'cat' for large files to avoid flooding your terminal",
          "'tail -f' is essential for monitoring log files in real-time",
          "In less, press '/' to search forward and '?' to search backward",
          "Combine: 'head -100 file | tail -20' to see lines 81-100",
        ],
        flashcards: [
          { front: "How do you follow a log file in real-time?", back: "tail -f /path/to/logfile" },
          { front: "How do you count only the number of lines in a file?", back: "wc -l filename" },
          { front: "What key exits the 'less' pager?", back: "q" },
          { front: "How do you display a file with line numbers?", back: "cat -n filename or nl filename" },
          { front: "How do you view the first 20 lines of a file?", back: "head -n 20 filename (or head -20 filename)" },
          { front: "What does 'tac' do?", back: "Displays file contents in reverse order (last line first) — it's 'cat' backwards" },
        ],
      },
      {
        slug: "grep-search",
        title: "Grep & Pattern Searching",
        content: `**grep (Global Regular Expression Print)** searches for patterns in files. It is one of the most powerful and frequently used Linux tools.\n\n**Basic usage:**\n- \`grep "error" logfile.txt\` — find lines containing "error"\n- \`grep -i "error" logfile.txt\` — case-insensitive search\n- \`grep -r "TODO" /project/\` — recursive search in directory\n- \`grep -n "pattern" file\` — show line numbers\n- \`grep -c "pattern" file\` — count matching lines\n- \`grep -v "pattern" file\` — invert match (show non-matching lines)\n- \`grep -l "pattern" *.txt\` — show only filenames with matches\n- \`grep -w "word" file\` — match whole words only\n\n**Regular expressions with grep:**\n- \`grep "^Start" file\` — lines starting with "Start"\n- \`grep "end$" file\` — lines ending with "end"\n- \`grep "c.t" file\` — matches cat, cot, cut (. = any single char)\n- \`grep "colou\\?r" file\` — matches color and colour\n- \`grep -E "cat|dog" file\` — extended regex (OR)\n- \`grep -E "[0-9]{3}" file\` — three consecutive digits\n\n**egrep** is equivalent to \`grep -E\` (extended regex).\n**fgrep** is equivalent to \`grep -F\` (fixed strings, no regex).\n\n**Combining with pipes:**\n- \`ps aux | grep nginx\` — find nginx processes\n- \`dmesg | grep -i usb\` — find USB-related kernel messages\n- \`cat /etc/passwd | grep -c "/bin/bash"\` — count bash users`,
        commands: [
          { cmd: "grep -rni 'error' /var/log/", desc: "Recursive, case-insensitive search with line numbers" },
          { cmd: "grep -v '^#' config.conf", desc: "Show non-comment lines" },
          { cmd: "grep -E '^[0-9]+' file", desc: "Lines starting with numbers" },
          { cmd: "grep -c 'pattern' file", desc: "Count matching lines" },
          { cmd: "grep -l 'TODO' *.py", desc: "List files containing TODO" },
          { cmd: "ps aux | grep apache", desc: "Find apache processes" },
        ],
        tips: [
          "Use 'grep -r' for searching through entire project directories",
          "Add '--color=auto' to highlight matches (usually default in modern systems)",
          "Use 'grep -P' for Perl-compatible regex (more powerful patterns)",
          "Combine grep with wc: 'grep -c pattern file' is faster than 'grep pattern file | wc -l'",
        ],
        flashcards: [
          { front: "How do you search recursively and case-insensitively?", back: "grep -ri 'pattern' /directory/" },
          { front: "How do you show only lines that do NOT match?", back: "grep -v 'pattern' file" },
          { front: "What regex matches the start of a line?", back: "^ (caret) — e.g., grep '^Start' file" },
          { front: "How do you count the number of matching lines?", back: "grep -c 'pattern' file" },
          { front: "What does grep -w do?", back: "Matches whole words only (not substrings)" },
          { front: "How do you search for multiple patterns (OR)?", back: "grep -E 'pattern1|pattern2' file" },
        ],
      },
      {
        slug: "sed-awk",
        title: "Sed & Awk",
        content: `**sed (Stream Editor)** performs text transformations on input streams or files.\n\n**Substitution (most common):**\n- \`sed 's/old/new/' file\` — replace first occurrence per line\n- \`sed 's/old/new/g' file\` — replace ALL occurrences (global)\n- \`sed -i 's/old/new/g' file\` — edit file in-place\n- \`sed -i.bak 's/old/new/g' file\` — in-place with backup\n\n**Line operations:**\n- \`sed '5d' file\` — delete line 5\n- \`sed '2,5d' file\` — delete lines 2-5\n- \`sed '/pattern/d' file\` — delete lines matching pattern\n- \`sed -n '10,20p' file\` — print only lines 10-20\n- \`sed '3i\\New line' file\` — insert text before line 3\n- \`sed '3a\\New line' file\` — append text after line 3\n\n**awk** is a powerful text processing language for column-based data.\n\n**Basic awk:**\n- \`awk '{print $1}' file\` — print first column\n- \`awk '{print $1, $3}' file\` — print columns 1 and 3\n- \`awk -F: '{print $1}' /etc/passwd\` — use : as field separator\n- \`awk '{print NR, $0}' file\` — print line numbers\n- \`awk '/pattern/ {print}' file\` — print lines matching pattern\n\n**Built-in variables:**\n- \`$0\` — entire line\n- \`$1, $2, ...\` — individual fields\n- \`NR\` — current line number\n- \`NF\` — number of fields in current line\n- \`FS\` — field separator\n\n**Examples:**\n- \`awk -F: '$3 >= 1000 {print $1}' /etc/passwd\` — users with UID >= 1000\n- \`df -h | awk '$5 > "80%" {print $1, $5}'\` — disks over 80% full\n- \`awk '{sum += $1} END {print sum}' numbers.txt\` — sum a column`,
        commands: [
          { cmd: "sed 's/foo/bar/g' file.txt", desc: "Replace all 'foo' with 'bar'" },
          { cmd: "sed -i '/^$/d' file.txt", desc: "Remove blank lines in-place" },
          { cmd: "awk -F: '{print $1}' /etc/passwd", desc: "Print all usernames" },
          { cmd: "awk '{print NF}' file", desc: "Print number of fields per line" },
          { cmd: "sed -n '10,20p' file", desc: "Print lines 10 through 20" },
        ],
        tips: [
          "Always test sed without -i first to preview changes",
          "Use sed -i.bak to create a backup before in-place editing",
          "awk is ideal for processing CSV, TSV, and columnar data",
          "Combine: 'grep pattern file | awk \"{print $2}\"' for filtered column extraction",
        ],
        flashcards: [
          { front: "How do you replace all occurrences of 'old' with 'new' in a file?", back: "sed 's/old/new/g' file (add -i for in-place)" },
          { front: "How do you print the first column of a file with awk?", back: "awk '{print $1}' file" },
          { front: "What does the 'g' flag mean in sed substitution?", back: "Global — replace all occurrences on each line, not just the first" },
          { front: "How do you set a custom field separator in awk?", back: "awk -F'delimiter' — e.g., awk -F: for colon-separated" },
          { front: "What is $0 in awk?", back: "The entire current line" },
          { front: "How do you delete blank lines with sed?", back: "sed '/^$/d' file" },
        ],
      },
      {
        slug: "sort-cut-uniq",
        title: "Sort, Cut & Uniq",
        content: `These filter commands are essential for processing and analyzing text data.\n\n**sort** — sort lines of text:\n- \`sort file.txt\` — alphabetical sort\n- \`sort -n file.txt\` — numeric sort\n- \`sort -r file.txt\` — reverse sort\n- \`sort -k2 file.txt\` — sort by second column\n- \`sort -t: -k3 -n /etc/passwd\` — sort passwd by UID\n- \`sort -u file.txt\` — sort and remove duplicates\n- \`sort -h file.txt\` — human-readable numeric sort (1K, 2M, 3G)\n\n**uniq** — report or filter duplicate lines (input must be sorted!):\n- \`sort file | uniq\` — remove duplicates\n- \`sort file | uniq -c\` — count occurrences\n- \`sort file | uniq -d\` — show only duplicates\n- \`sort file | uniq -u\` — show only unique lines\n\n**cut** — extract sections from lines:\n- \`cut -d: -f1 /etc/passwd\` — extract first field (delimiter :)\n- \`cut -d, -f1,3 data.csv\` — extract columns 1 and 3 from CSV\n- \`cut -c1-10 file\` — extract characters 1-10\n- \`cut -f2-\` — extract from field 2 to end\n\n**tr (Translate)** — translate or delete characters:\n- \`tr 'a-z' 'A-Z'\` — convert lowercase to uppercase\n- \`tr -d '\\n'\` — delete newlines\n- \`tr -s ' '\` — squeeze repeated spaces\n- \`echo "hello" | tr 'el' 'ip'\` — output: hippo\n\n**Common pipeline patterns:**\n- \`cat access.log | cut -d' ' -f1 | sort | uniq -c | sort -rn | head\` — top 10 IP addresses\n- \`cut -d: -f7 /etc/passwd | sort | uniq -c | sort -rn\` — count login shells`,
        commands: [
          { cmd: "sort -t: -k3 -n /etc/passwd", desc: "Sort passwd file by UID numerically" },
          { cmd: "sort file | uniq -c | sort -rn", desc: "Count and rank occurrences" },
          { cmd: "cut -d: -f1,7 /etc/passwd", desc: "Extract username and shell from passwd" },
          { cmd: "tr 'A-Z' 'a-z' < file.txt", desc: "Convert to lowercase" },
          { cmd: "cat log | cut -d' ' -f1 | sort -u", desc: "Get unique values from first column" },
        ],
        tips: [
          "uniq only removes ADJACENT duplicates — always sort first",
          "Use 'sort -u' as a shortcut for 'sort | uniq'",
          "cut is faster than awk for simple column extraction",
          "The 'tr' command reads from stdin — use pipes or redirection",
        ],
        flashcards: [
          { front: "Why must you sort before using uniq?", back: "uniq only removes adjacent duplicate lines — sorting groups duplicates together" },
          { front: "How do you count occurrences of each line?", back: "sort file | uniq -c" },
          { front: "How do you extract the first field from a colon-delimited file?", back: "cut -d: -f1 filename" },
          { front: "How do you sort numerically in reverse order?", back: "sort -rn file" },
          { front: "What does 'tr -s' do?", back: "Squeezes (compresses) repeated characters into one" },
          { front: "How do you convert a file to uppercase?", back: "tr 'a-z' 'A-Z' < file" },
        ],
      },
    ],
  },
  {
    key: "piping-redirection",
    name: "Piping & Redirection",
    level: "beginner",
    topics: [
      {
        slug: "pipes-redirection",
        title: "Pipes & Redirection",
        content: `Piping and redirection are core concepts that make the Linux command line incredibly powerful by connecting commands together.\n\n**Standard streams:**\n- **stdin (0)** — standard input (keyboard by default)\n- **stdout (1)** — standard output (terminal by default)\n- **stderr (2)** — standard error (terminal by default)\n\n**Output redirection:**\n- \`command > file\` — redirect stdout to file (overwrite)\n- \`command >> file\` — append stdout to file\n- \`command 2> file\` — redirect stderr to file\n- \`command 2>&1\` — redirect stderr to stdout\n- \`command &> file\` — redirect both stdout and stderr to file\n- \`command > /dev/null 2>&1\` — discard all output\n\n**Input redirection:**\n- \`command < file\` — use file as stdin\n- \`sort < unsorted.txt > sorted.txt\` — input from file, output to file\n\n**Here documents:**\n- \`cat << EOF\n... text ...\nEOF\` — multi-line input\n\n**Pipes (|):**\nPipes send stdout of one command as stdin to the next.\n- \`ls -l | grep ".txt"\` — filter ls output\n- \`cat log | sort | uniq -c | sort -rn | head\` — pipeline chain\n- \`command1 | tee file | command2\` — tee splits output to file AND pipe\n\n**tee** writes to both stdout and a file:\n- \`ls | tee filelist.txt\` — display and save\n- \`ls | tee -a filelist.txt\` — append mode\n\n**xargs** converts stdin to command arguments:\n- \`find . -name "*.tmp" | xargs rm\` — delete found files\n- \`cat urls.txt | xargs -n1 curl -O\` — download each URL`,
        commands: [
          { cmd: "command > output.txt 2>&1", desc: "Redirect stdout and stderr to file" },
          { cmd: "command | tee log.txt", desc: "Display output and save to file" },
          { cmd: "find . -name '*.log' | xargs rm", desc: "Delete files found by find" },
          { cmd: "echo 'text' >> file.txt", desc: "Append text to file" },
          { cmd: "command > /dev/null 2>&1", desc: "Suppress all output" },
        ],
        tips: [
          "/dev/null is the 'black hole' — anything sent there is discarded",
          "Use 'tee' when you want to see output AND save it simultaneously",
          "Pipes only pass stdout — use 2>&1 before the pipe to include stderr",
          "xargs is safer with -0 flag when filenames contain spaces: find ... -print0 | xargs -0 rm",
        ],
        flashcards: [
          { front: "What does > do vs >>?", back: "> overwrites the file, >> appends to the file" },
          { front: "How do you redirect stderr to a file?", back: "command 2> errorfile" },
          { front: "What does 2>&1 mean?", back: "Redirect stderr (fd 2) to wherever stdout (fd 1) is going" },
          { front: "What does the pipe (|) do?", back: "Sends stdout of the left command as stdin to the right command" },
          { front: "What is /dev/null?", back: "A special file that discards all data written to it (the 'black hole')" },
          { front: "What does 'tee' do?", back: "Reads stdin and writes it to both stdout and one or more files" },
        ],
      },
    ],
  },
  {
    key: "process-management",
    name: "Process Management",
    level: "beginner",
    topics: [
      {
        slug: "processes",
        title: "Process Management",
        content: `Every running program in Linux is a process with a unique PID (Process ID). Understanding process management is critical for system administration.\n\n**Viewing processes:**\n- \`ps\` — show processes for current session\n- \`ps aux\` — show ALL processes with details\n  - USER, PID, %CPU, %MEM, VSZ, RSS, TTY, STAT, START, TIME, COMMAND\n- \`ps -ef\` — full-format listing (alternative style)\n- \`ps aux --sort=-%mem | head\` — top memory consumers\n\n**Process states (STAT column):**\n- R — Running\n- S — Sleeping (waiting for event)\n- D — Uninterruptible sleep (usually I/O)\n- Z — Zombie (terminated but not reaped)\n- T — Stopped\n\n**top / htop** — real-time process monitoring:\n- \`top\` — built-in process monitor\n  - Press M (sort by memory), P (sort by CPU), k (kill), q (quit)\n- \`htop\` — enhanced interactive process viewer (install separately)\n\n**Killing processes:**\n- \`kill PID\` — send SIGTERM (graceful termination)\n- \`kill -9 PID\` — send SIGKILL (force kill, cannot be caught)\n- \`kill -HUP PID\` — send SIGHUP (reload config)\n- \`killall processname\` — kill all processes by name\n- \`pkill -f pattern\` — kill processes matching pattern\n\n**Job control:**\n- \`CTRL+C\` — send SIGINT (interrupt/cancel current process)\n- \`CTRL+Z\` — suspend current process (send to background)\n- \`jobs\` — list background jobs\n- \`bg %1\` — resume job 1 in background\n- \`fg %1\` — bring job 1 to foreground\n- \`command &\` — start command in background\n\n**nohup & disown:**\n- \`nohup command &\` — run command immune to hangups\n- \`disown %1\` — detach job from terminal`,
        commands: [
          { cmd: "ps aux | grep nginx", desc: "Find nginx processes" },
          { cmd: "top -bn1 | head -20", desc: "Snapshot of top processes" },
          { cmd: "kill -9 1234", desc: "Force kill process 1234" },
          { cmd: "killall -HUP apache2", desc: "Reload Apache config" },
          { cmd: "nohup ./script.sh &", desc: "Run script immune to hangup" },
          { cmd: "jobs -l", desc: "List background jobs with PIDs" },
        ],
        tips: [
          "Always try 'kill PID' (SIGTERM) before 'kill -9' (SIGKILL)",
          "Zombie processes cannot be killed — kill their parent process instead",
          "Use 'pgrep' to find PIDs: pgrep -u root sshd",
          "Use 'nice' and 'renice' to adjust process priority",
        ],
        flashcards: [
          { front: "What is the difference between kill and kill -9?", back: "kill sends SIGTERM (graceful shutdown), kill -9 sends SIGKILL (force kill, cannot be caught)" },
          { front: "What does CTRL+Z do?", back: "Suspends the current foreground process and puts it in the background" },
          { front: "How do you run a command in the background?", back: "Append & to the command: command &" },
          { front: "What is a zombie process?", back: "A process that has terminated but its parent hasn't read its exit status" },
          { front: "What does 'ps aux' show?", back: "All running processes with details (user, PID, CPU%, MEM%, command, etc.)" },
          { front: "How do you bring a background job to the foreground?", back: "fg %jobnumber (e.g., fg %1)" },
        ],
      },
    ],
  },
  {
    key: "vim-editor",
    name: "Vi/Vim Editor",
    level: "beginner",
    topics: [
      {
        slug: "vim",
        title: "Vi/Vim Editor",
        content: `Vi (and its improved version Vim) is the standard text editor available on virtually every Linux system. It has three main modes.\n\n**Modes:**\n- **Normal mode** (default) — navigate and manipulate text\n- **Insert mode** — type/edit text (press i, a, o to enter)\n- **Command mode** — execute commands (press : from normal mode)\n\n**Entering Insert mode:**\n- \`i\` — insert before cursor\n- \`a\` — insert after cursor\n- \`I\` — insert at beginning of line\n- \`A\` — insert at end of line\n- \`o\` — open new line below\n- \`O\` — open new line above\n- Press \`ESC\` to return to normal mode\n\n**Navigation (Normal mode):**\n- h, j, k, l — left, down, up, right\n- w — next word, b — previous word\n- 0 — beginning of line, $ — end of line\n- gg — go to first line, G — go to last line\n- :n — go to line n (e.g., :42)\n- CTRL+f — page down, CTRL+b — page up\n\n**Editing (Normal mode):**\n- x — delete character\n- dd — delete (cut) entire line\n- yy — yank (copy) entire line\n- p — paste after cursor, P — paste before\n- u — undo, CTRL+r — redo\n- . — repeat last command\n- dw — delete word, d$ — delete to end of line\n- cw — change word (delete + enter insert mode)\n\n**Search & Replace:**\n- /pattern — search forward\n- ?pattern — search backward\n- n — next match, N — previous match\n- :%s/old/new/g — replace all in file\n- :s/old/new/g — replace all in current line\n\n**Saving & Quitting (Command mode):**\n- :w — save (write)\n- :q — quit\n- :wq or :x — save and quit\n- :q! — quit without saving\n- :w filename — save as different filename`,
        commands: [
          { cmd: "vim filename", desc: "Open file in Vim" },
          { cmd: "vim +42 file", desc: "Open file at line 42" },
          { cmd: "vim -R file", desc: "Open file in read-only mode" },
          { cmd: "vimdiff file1 file2", desc: "Compare two files side by side" },
        ],
        tips: [
          "If you get stuck, press ESC multiple times then type :q! to force quit",
          "Use 'vimtutor' command to learn Vim interactively (built-in tutorial)",
          "In normal mode, numbers repeat commands: 5dd deletes 5 lines",
          "Use :set number to show line numbers, :set nonumber to hide them",
        ],
        flashcards: [
          { front: "How do you save and quit in Vim?", back: ":wq or :x (in command mode)" },
          { front: "How do you quit without saving?", back: ":q!" },
          { front: "How do you enter insert mode?", back: "Press i (insert before cursor), a (after cursor), o (new line below)" },
          { front: "How do you delete an entire line?", back: "dd in normal mode" },
          { front: "How do you undo in Vim?", back: "u in normal mode" },
          { front: "How do you search and replace all occurrences in a file?", back: ":%s/old/new/g" },
          { front: "How do you go to a specific line number?", back: ":n (e.g., :42 to go to line 42)" },
          { front: "What are the three modes of Vim?", back: "Normal (default), Insert (editing text), Command (: commands)" },
        ],
      },
    ],
  },
  {
    key: "user-management",
    name: "Users & Groups",
    level: "intermediate",
    topics: [
      {
        slug: "users-groups",
        title: "User & Group Management",
        content: `Linux is a multi-user OS. Users and groups control access and permissions. User info is stored in /etc/passwd, passwords in /etc/shadow, groups in /etc/group.\n\n**/etc/passwd format:** username:x:UID:GID:comment:home:shell\nExample: \`alice:x:1001:1001:Alice Smith:/home/alice:/bin/bash\`\n\n**User management:**\n- \`useradd username\` — create user (basic)\n- \`useradd -m -s /bin/bash -G sudo username\` — create with home dir, bash shell, add to sudo group\n- \`usermod -aG groupname username\` — add user to group\n- \`usermod -L username\` — lock user account\n- \`usermod -U username\` — unlock user account\n- \`userdel username\` — delete user\n- \`userdel -r username\` — delete user and home directory\n- \`passwd username\` — set/change password\n- \`passwd -l username\` — lock password\n\n**Group management:**\n- \`groupadd groupname\` — create group\n- \`groupdel groupname\` — delete group\n- \`groupmod -n newname oldname\` — rename group\n- \`groups username\` — show user's groups\n- \`id username\` — show UID, GID, and all groups\n\n**sudo (Superuser Do):**\n- \`sudo command\` — run command as root\n- \`sudo -u user command\` — run as specific user\n- \`sudo -i\` — open root shell\n- \`visudo\` — safely edit /etc/sudoers\n- sudoers syntax: \`username ALL=(ALL:ALL) ALL\`\n\n**Key files:**\n- /etc/passwd — user account info\n- /etc/shadow — encrypted passwords\n- /etc/group — group definitions\n- /etc/sudoers — sudo privileges\n- /etc/skel/ — template for new user home directories`,
        commands: [
          { cmd: "useradd -m -s /bin/bash alice", desc: "Create user with home dir and bash" },
          { cmd: "usermod -aG docker,sudo alice", desc: "Add user to multiple groups" },
          { cmd: "passwd alice", desc: "Set password for alice" },
          { cmd: "id alice", desc: "Show user's UID, GID, and groups" },
          { cmd: "groupadd developers", desc: "Create a new group" },
          { cmd: "sudo visudo", desc: "Safely edit sudoers file" },
        ],
        tips: [
          "Always use 'usermod -aG' (append) — without -a it REPLACES all groups",
          "Use 'visudo' to edit sudoers — it validates syntax before saving",
          "UIDs below 1000 are typically reserved for system accounts",
          "After adding a user to a group, they need to log out and back in",
        ],
        flashcards: [
          { front: "What file stores user account information?", back: "/etc/passwd" },
          { front: "How do you add an existing user to a group?", back: "usermod -aG groupname username (-a is critical to append)" },
          { front: "Where are encrypted passwords stored?", back: "/etc/shadow" },
          { front: "How do you create a user with a home directory?", back: "useradd -m username" },
          { front: "What does 'id username' show?", back: "The user's UID, primary GID, and all group memberships" },
          { front: "Why use visudo instead of directly editing /etc/sudoers?", back: "visudo checks for syntax errors before saving, preventing lockout" },
        ],
      },
    ],
  },
  {
    key: "system-admin",
    name: "System Administration",
    level: "intermediate",
    topics: [
      {
        slug: "package-management",
        title: "Package Management",
        content: `Package managers automate installing, updating, and removing software. Different distro families use different tools.\n\n**Debian/Ubuntu (APT + dpkg):**\n- \`apt update\` — refresh package index\n- \`apt upgrade\` — upgrade all installed packages\n- \`apt install package\` — install a package\n- \`apt remove package\` — remove package (keep config)\n- \`apt purge package\` — remove package and config\n- \`apt search keyword\` — search for packages\n- \`apt show package\` — package details\n- \`apt autoremove\` — remove unused dependencies\n- \`dpkg -i package.deb\` — install local .deb file\n- \`dpkg -l\` — list installed packages\n- \`dpkg -L package\` — list files installed by package\n\n**RHEL/CentOS/Fedora (DNF/YUM + rpm):**\n- \`dnf install package\` — install\n- \`dnf remove package\` — remove\n- \`dnf update\` — update all packages\n- \`dnf search keyword\` — search\n- \`dnf info package\` — package details\n- \`dnf list installed\` — list installed packages\n- \`rpm -i package.rpm\` — install local .rpm\n- \`rpm -qa\` — list all installed packages\n- \`rpm -ql package\` — list files from package\n\n**Key concepts:**\n- **Repository**: server hosting packages; configured in /etc/apt/sources.list (Debian) or /etc/yum.repos.d/ (RHEL)\n- **Dependencies**: packages required by other packages; managers resolve these automatically\n- **GPG keys**: packages are signed for authenticity`,
        commands: [
          { cmd: "sudo apt update && sudo apt upgrade -y", desc: "Update package list and upgrade all" },
          { cmd: "apt search nginx", desc: "Search for nginx packages" },
          { cmd: "dpkg -L nginx", desc: "List files installed by nginx" },
          { cmd: "sudo dnf install httpd", desc: "Install Apache on RHEL" },
          { cmd: "rpm -qa | grep ssh", desc: "Find installed SSH packages" },
        ],
        tips: [
          "Always run 'apt update' before 'apt install' to get latest package info",
          "Use 'apt list --upgradable' to see what will be upgraded",
          "On RHEL systems, 'yum' is the older tool, 'dnf' is the modern replacement",
          "Use 'apt-cache policy package' to see available versions and sources",
        ],
        flashcards: [
          { front: "What is the package manager for Debian/Ubuntu?", back: "APT (Advanced Package Tool) with dpkg as the underlying tool" },
          { front: "What is the package manager for RHEL/CentOS?", back: "DNF (or YUM on older versions) with rpm as the underlying tool" },
          { front: "How do you install a .deb file directly?", back: "dpkg -i package.deb (then apt install -f to fix dependencies)" },
          { front: "Why run 'apt update' before installing?", back: "It refreshes the local package index so you get the latest available versions" },
          { front: "Where are APT repositories configured?", back: "/etc/apt/sources.list and /etc/apt/sources.list.d/" },
          { front: "How do you list all installed packages on Debian?", back: "dpkg -l or apt list --installed" },
        ],
      },
      {
        slug: "systemd-services",
        title: "Systemd & Services",
        content: `Systemd is the init system and service manager on most modern Linux distributions. It manages system startup and services.\n\n**systemctl** — control the systemd system and service manager:\n\n**Service management:**\n- \`systemctl start service\` — start a service\n- \`systemctl stop service\` — stop a service\n- \`systemctl restart service\` — restart a service\n- \`systemctl reload service\` — reload config without restart\n- \`systemctl status service\` — show service status and recent logs\n- \`systemctl enable service\` — start on boot\n- \`systemctl disable service\` — don't start on boot\n- \`systemctl is-active service\` — check if running\n- \`systemctl is-enabled service\` — check if enabled on boot\n- \`systemctl list-units --type=service\` — list all services\n- \`systemctl list-units --failed\` — show failed services\n\n**journalctl** — query the systemd journal (logs):\n- \`journalctl\` — show all logs\n- \`journalctl -u service\` — logs for specific service\n- \`journalctl -f\` — follow (like tail -f)\n- \`journalctl --since "1 hour ago"\`\n- \`journalctl -b\` — logs since last boot\n- \`journalctl -p err\` — only error priority and above\n- \`journalctl --disk-usage\` — see log disk usage\n\n**Targets (runlevels):**\n- \`systemctl get-default\` — show default target\n- \`systemctl set-default multi-user.target\` — boot to CLI\n- \`systemctl set-default graphical.target\` — boot to GUI\n\n**Unit file locations:**\n- /etc/systemd/system/ — custom/override units (highest priority)\n- /usr/lib/systemd/system/ — package-installed units\n- After modifying unit files: \`systemctl daemon-reload\``,
        commands: [
          { cmd: "sudo systemctl restart nginx", desc: "Restart nginx" },
          { cmd: "systemctl status sshd", desc: "Check SSH service status" },
          { cmd: "sudo systemctl enable --now docker", desc: "Enable and start Docker" },
          { cmd: "journalctl -u nginx --since today", desc: "Today's nginx logs" },
          { cmd: "systemctl list-units --failed", desc: "Show failed services" },
        ],
        tips: [
          "'enable --now' combines 'enable' and 'start' in one command",
          "Use 'systemctl cat service' to see the unit file contents",
          "Use 'systemctl mask service' to completely prevent a service from starting",
          "journalctl -xe gives detailed info about recent errors",
        ],
        flashcards: [
          { front: "How do you start a service and enable it on boot in one command?", back: "systemctl enable --now servicename" },
          { front: "How do you view logs for a specific service?", back: "journalctl -u servicename" },
          { front: "What does 'systemctl daemon-reload' do?", back: "Reloads systemd manager configuration after modifying unit files" },
          { front: "How do you check why a service failed?", back: "systemctl status servicename and journalctl -xe -u servicename" },
          { front: "Where do custom systemd unit files go?", back: "/etc/systemd/system/" },
          { front: "What is the difference between 'restart' and 'reload'?", back: "restart stops and starts the process; reload re-reads config without stopping" },
        ],
      },
      {
        slug: "cron-scheduling",
        title: "Cron Jobs & Scheduling",
        content: `Cron is the time-based job scheduler in Linux. It runs commands at specified times and intervals.\n\n**Crontab format:**\n\`* * * * * command\`\n\`minute(0-59) hour(0-23) day-of-month(1-31) month(1-12) day-of-week(0-7, 0&7=Sun)\`\n\n**Examples:**\n- \`0 2 * * * /backup.sh\` — daily at 2:00 AM\n- \`*/15 * * * * /check.sh\` — every 15 minutes\n- \`0 9-17 * * 1-5 /work.sh\` — hourly 9AM-5PM weekdays\n- \`0 0 1 * * /monthly.sh\` — first day of each month at midnight\n- \`@reboot /startup.sh\` — run at system startup\n- \`@hourly /script.sh\` — shorthand for 0 * * * *\n- \`@daily /script.sh\` — shorthand for 0 0 * * *\n\n**Managing crontabs:**\n- \`crontab -e\` — edit your crontab\n- \`crontab -l\` — list your crontab entries\n- \`crontab -r\` — remove your crontab\n- \`sudo crontab -u alice -e\` — edit another user's crontab\n\n**System cron directories:**\n- /etc/crontab — system crontab\n- /etc/cron.d/ — additional cron files\n- /etc/cron.daily/ — scripts run daily\n- /etc/cron.hourly/ — scripts run hourly\n- /etc/cron.weekly/ — scripts run weekly\n- /etc/cron.monthly/ — scripts run monthly\n\n**at** — one-time scheduled jobs:\n- \`at 2:00 PM\` then type commands, CTRL+D\n- \`at now + 30 minutes\`\n- \`atq\` — list pending at jobs\n- \`atrm jobnumber\` — remove an at job\n\n**systemd timers** are the modern alternative to cron with better logging and dependency management.`,
        commands: [
          { cmd: "crontab -e", desc: "Edit your crontab" },
          { cmd: "crontab -l", desc: "List your scheduled jobs" },
          { cmd: "sudo crontab -u root -l", desc: "List root's crontab" },
          { cmd: "at now + 2 hours", desc: "Schedule a one-time job" },
          { cmd: "systemctl list-timers", desc: "List active systemd timers" },
        ],
        tips: [
          "Use full paths in cron jobs — cron has a minimal PATH",
          "Redirect output to a log: 0 2 * * * /backup.sh >> /var/log/backup.log 2>&1",
          "Use 'crontab -e' not 'vi /var/spool/cron/...' to edit safely",
          "Test your cron schedule with crontab.guru website",
        ],
        flashcards: [
          { front: "What does '*/15 * * * *' mean in crontab?", back: "Run every 15 minutes" },
          { front: "What are the five fields in a crontab entry?", back: "minute, hour, day-of-month, month, day-of-week" },
          { front: "How do you list your cron jobs?", back: "crontab -l" },
          { front: "What does @reboot do in crontab?", back: "Runs the command once when the system starts up" },
          { front: "Where are system-wide cron scripts stored?", back: "/etc/cron.d/, /etc/cron.daily/, /etc/cron.hourly/, etc." },
          { front: "How do you schedule a one-time task?", back: "Use the 'at' command: at 2:00 PM" },
        ],
      },
    ],
  },
  {
    key: "networking",
    name: "Networking",
    level: "intermediate",
    topics: [
      {
        slug: "network-basics",
        title: "Network Configuration & Tools",
        content: `Network management is essential for Linux administration. Modern systems use \`ip\` commands (replacing older \`ifconfig\`).\n\n**Viewing network info:**\n- \`ip addr show\` (or \`ip a\`) — show IP addresses\n- \`ip link show\` — show network interfaces\n- \`ip route show\` — show routing table\n- \`hostname -I\` — quick way to see your IP\n- \`cat /etc/resolv.conf\` — DNS configuration\n\n**Configuring interfaces:**\n- \`sudo ip addr add 192.168.1.100/24 dev eth0\` — add IP\n- \`sudo ip link set eth0 up/down\` — enable/disable interface\n- \`sudo ip route add default via 192.168.1.1\` — set default gateway\n- Persistent config: /etc/network/interfaces (Debian) or /etc/sysconfig/network-scripts/ (RHEL) or Netplan /etc/netplan/ (Ubuntu)\n\n**Diagnostic tools:**\n- \`ping host\` — test connectivity (ICMP echo)\n- \`traceroute host\` — trace packet route\n- \`dig domain\` — DNS lookup\n- \`nslookup domain\` — DNS query\n- \`host domain\` — simple DNS lookup\n- \`curl -I https://example.com\` — HTTP headers\n- \`wget https://example.com/file\` — download file\n\n**Connection info:**\n- \`ss -tulnp\` — show listening ports (replaced netstat)\n  - t=TCP, u=UDP, l=listening, n=numeric, p=process\n- \`ss -s\` — socket statistics summary\n- \`netstat -tulnp\` — older alternative to ss\n\n**Firewall (firewalld / iptables / ufw):**\n- \`sudo ufw enable\` — enable firewall (Ubuntu)\n- \`sudo ufw allow 22/tcp\` — allow SSH\n- \`sudo ufw status\` — show rules\n- \`sudo firewall-cmd --add-service=http --permanent\` — RHEL\n- \`sudo iptables -L\` — list iptables rules`,
        commands: [
          { cmd: "ip a", desc: "Show all IP addresses" },
          { cmd: "ss -tulnp", desc: "Show listening ports with processes" },
          { cmd: "ping -c 4 8.8.8.8", desc: "Ping Google DNS 4 times" },
          { cmd: "dig example.com", desc: "DNS lookup" },
          { cmd: "curl -O https://example.com/file.tar.gz", desc: "Download a file" },
          { cmd: "sudo ufw allow 443/tcp", desc: "Allow HTTPS through firewall" },
        ],
        tips: [
          "'ip' commands replace deprecated ifconfig, route, netstat",
          "Use 'ss -tulnp' instead of 'netstat -tulnp' on modern systems",
          "ping -c N limits the count instead of pinging forever",
          "Use 'dig +short domain' for quick DNS answers",
        ],
        flashcards: [
          { front: "How do you view all IP addresses on the system?", back: "ip addr show (or ip a)" },
          { front: "How do you check which ports are listening?", back: "ss -tulnp" },
          { front: "What replaced ifconfig in modern Linux?", back: "The 'ip' command (ip addr, ip link, ip route)" },
          { front: "How do you perform a DNS lookup?", back: "dig domain or nslookup domain or host domain" },
          { front: "What does 'ss -tulnp' show?", back: "TCP/UDP listening sockets with numeric ports and process names" },
          { front: "How do you allow SSH through UFW firewall?", back: "sudo ufw allow 22/tcp (or sudo ufw allow ssh)" },
        ],
      },
      {
        slug: "ssh-remote",
        title: "SSH & Remote Administration",
        content: `SSH (Secure Shell) provides encrypted remote access and is the primary tool for remote Linux administration.\n\n**Basic usage:**\n- \`ssh user@hostname\` — connect to remote host\n- \`ssh -p 2222 user@host\` — connect on non-standard port\n- \`ssh user@host command\` — run single command remotely\n\n**SSH key authentication (more secure than passwords):**\n1. \`ssh-keygen -t ed25519\` — generate key pair\n2. \`ssh-copy-id user@host\` — copy public key to remote\n3. Now login is passwordless and more secure\n\n**Key files:**\n- ~/.ssh/id_ed25519 — private key (chmod 600!)\n- ~/.ssh/id_ed25519.pub — public key\n- ~/.ssh/authorized_keys — remote file listing allowed public keys\n- ~/.ssh/known_hosts — fingerprints of known hosts\n- ~/.ssh/config — client configuration\n\n**SSH config (~/.ssh/config):**\n\`\`\`\nHost myserver\n  HostName 192.168.1.100\n  User alice\n  Port 2222\n  IdentityFile ~/.ssh/id_ed25519\n\`\`\`\nThen just: \`ssh myserver\`\n\n**SCP (Secure Copy):**\n- \`scp file.txt user@host:/path/\` — copy to remote\n- \`scp user@host:/path/file.txt .\` — copy from remote\n- \`scp -r directory/ user@host:/path/\` — copy directory\n\n**rsync (better for large/repeated transfers):**\n- \`rsync -avz source/ user@host:/dest/\` — sync with compression\n- \`rsync -avz --delete source/ dest/\` — mirror (delete extra files)\n\n**SSH tunneling:**\n- \`ssh -L 8080:localhost:80 user@host\` — local port forwarding\n- \`ssh -R 8080:localhost:80 user@host\` — remote port forwarding\n- \`ssh -D 1080 user@host\` — SOCKS proxy`,
        commands: [
          { cmd: "ssh-keygen -t ed25519", desc: "Generate SSH key pair" },
          { cmd: "ssh-copy-id user@server", desc: "Copy public key to server" },
          { cmd: "scp -r project/ user@host:~/", desc: "Copy directory to remote" },
          { cmd: "rsync -avz /data/ user@host:/backup/", desc: "Sync data to remote" },
          { cmd: "ssh -L 3306:localhost:3306 user@dbhost", desc: "Tunnel MySQL port" },
        ],
        tips: [
          "Always use key-based auth in production — disable password auth in sshd_config",
          "Use ed25519 keys — they're more secure and faster than RSA",
          "Set permissions: chmod 700 ~/.ssh, chmod 600 ~/.ssh/id_*",
          "Use ssh-agent to avoid retyping passphrases: eval $(ssh-agent) && ssh-add",
        ],
        flashcards: [
          { front: "How do you generate an SSH key pair?", back: "ssh-keygen -t ed25519 (or ssh-keygen -t rsa -b 4096)" },
          { front: "What permissions should SSH private keys have?", back: "600 (read/write for owner only)" },
          { front: "How do you copy your public key to a remote server?", back: "ssh-copy-id user@hostname" },
          { front: "What is the difference between scp and rsync?", back: "rsync is incremental (only transfers changes), supports compression, and can resume" },
          { front: "What does 'ssh -L 8080:localhost:80' do?", back: "Creates a local port forward — local port 8080 tunnels to remote port 80" },
          { front: "Where are SSH authorized keys stored on the server?", back: "~/.ssh/authorized_keys" },
        ],
      },
    ],
  },
  {
    key: "storage",
    name: "Disk & Storage",
    level: "intermediate",
    topics: [
      {
        slug: "disk-management",
        title: "Disk & Storage Management",
        content: `Managing disks, partitions, and filesystems is a core sysadmin skill.\n\n**Viewing disk info:**\n- \`df -h\` — show filesystem disk space usage (human-readable)\n- \`du -sh /path\` — show directory size\n- \`du -sh ./*\` — size of each item in current directory\n- \`lsblk\` — list block devices (disks and partitions)\n- \`fdisk -l\` — detailed partition table\n- \`blkid\` — show UUIDs and filesystem types\n\n**Partitioning:**\n- \`fdisk /dev/sdb\` — partition a disk (MBR)\n- \`gdisk /dev/sdb\` — partition a disk (GPT)\n- \`parted /dev/sdb\` — GNU partition editor\n\n**Filesystems:**\n- \`mkfs.ext4 /dev/sdb1\` — create ext4 filesystem\n- \`mkfs.xfs /dev/sdb1\` — create XFS filesystem\n- \`fsck /dev/sdb1\` — check and repair filesystem (unmount first!)\n\n**Mounting:**\n- \`mount /dev/sdb1 /mnt/data\` — mount partition\n- \`umount /mnt/data\` — unmount\n- \`mount -a\` — mount all entries in /etc/fstab\n- /etc/fstab — persistent mount configuration\n  Format: device mountpoint fstype options dump pass\n  Example: \`UUID=abc-123 /data ext4 defaults 0 2\`\n\n**LVM (Logical Volume Manager):**\nLVM adds flexibility by abstracting physical disks.\n- **PV (Physical Volume)**: \`pvcreate /dev/sdb\`\n- **VG (Volume Group)**: \`vgcreate myvg /dev/sdb\`\n- **LV (Logical Volume)**: \`lvcreate -L 10G -n mylv myvg\`\n- Resize: \`lvextend -L +5G /dev/myvg/mylv\` then \`resize2fs /dev/myvg/mylv\`\n- Display: \`pvdisplay\`, \`vgdisplay\`, \`lvdisplay\`\n\n**Swap:**\n- \`mkswap /dev/sdb2\` — create swap\n- \`swapon /dev/sdb2\` — enable swap\n- \`free -h\` — check memory and swap usage`,
        commands: [
          { cmd: "df -h", desc: "Show disk space usage" },
          { cmd: "du -sh /var/log", desc: "Show /var/log total size" },
          { cmd: "lsblk", desc: "List all block devices" },
          { cmd: "sudo mount /dev/sdb1 /mnt/usb", desc: "Mount a partition" },
          { cmd: "sudo lvextend -L +10G /dev/vg/lv", desc: "Extend logical volume" },
          { cmd: "free -h", desc: "Show memory and swap usage" },
        ],
        tips: [
          "Always use UUID in /etc/fstab — device names like /dev/sdb can change",
          "Never run fsck on a mounted filesystem",
          "Use 'df -i' to check inode usage — you can run out of inodes before space",
          "LVM allows online resizing — great for servers that can't have downtime",
        ],
        flashcards: [
          { front: "How do you check disk space usage?", back: "df -h (human-readable)" },
          { front: "How do you check a directory's size?", back: "du -sh /path" },
          { front: "What file configures persistent mounts?", back: "/etc/fstab" },
          { front: "What are the three layers of LVM?", back: "Physical Volume (PV) → Volume Group (VG) → Logical Volume (LV)" },
          { front: "How do you list all block devices?", back: "lsblk" },
          { front: "Why use UUID instead of /dev/sdX in fstab?", back: "Device names can change between boots; UUIDs are persistent" },
        ],
      },
    ],
  },
  {
    key: "scripting",
    name: "Bash Scripting",
    level: "intermediate",
    topics: [
      {
        slug: "bash-scripting",
        title: "Bash Scripting Basics",
        content: "Bash scripting automates repetitive tasks. Scripts are text files containing shell commands.\\n\\n**Creating a script:**\\n1. Start with shebang: `#!/bin/bash`\\n2. Make executable: `chmod +x script.sh`\\n3. Run: `./script.sh` or `bash script.sh`\\n\\n**Variables:**\\n- `NAME=\"Alice\"` — assign (no spaces around =)\\n- `echo $NAME` or `echo ${NAME}` — use variable\\n- `readonly PI=3.14` — constant\\n- `$1, $2, ...` — positional parameters (arguments)\\n- `$#` — number of arguments\\n- `$@` — all arguments\\n- `$?` — exit status of last command (0=success)\\n- `$$` — current script's PID\\n\\n**Conditionals:**\\n```bash\\nif [ \"$x\" -gt 10 ]; then\\n  echo \"big\"\\nelif [ \"$x\" -eq 10 ]; then\\n  echo \"ten\"\\nelse\\n  echo \"small\"\\nfi\\n```\\n\\n**Test operators:**\\n- Numeric: -eq, -ne, -gt, -lt, -ge, -le\\n- String: = (equal), != (not equal), -z (empty), -n (not empty)\\n- File: -f (is file), -d (is dir), -e (exists), -r (readable), -w (writable), -x (executable)\\n\\n**Loops:**\\n```bash\\nfor file in *.txt; do\\n  echo \"Processing $file\"\\ndone\\n\\nfor i in {1..10}; do echo $i; done\\n\\nwhile [ $count -lt 10 ]; do\\n  count=$((count + 1))\\ndone\\n```\\n\\n**Functions:**\\n```bash\\ngreet() {\\n  echo \"Hello, $1!\"\\n}\\ngreet \"World\"\\n```\\n\\n**Command substitution:**\\n- `TODAY=$(date +%Y-%m-%d)`\\n- `FILES=$(ls *.txt)`\\n\\n**Useful patterns:**\\n- `set -e` — exit on error\\n- `set -u` — error on undefined variables\\n- `set -o pipefail` — catch pipe failures",
        commands: [
          { cmd: "#!/bin/bash", desc: "Shebang — tells system to use bash" },
          { cmd: "chmod +x script.sh", desc: "Make script executable" },
          { cmd: "bash -x script.sh", desc: "Run with debug output" },
          { cmd: "shellcheck script.sh", desc: "Lint/check script for errors" },
        ],
        tips: [
          "Always quote variables: \"$var\" prevents word splitting issues",
          "Use 'set -euo pipefail' at the start of scripts for safety",
          "Use [[ ]] instead of [ ] for more features and fewer gotchas",
          "Use 'shellcheck' tool to find bugs and bad practices in your scripts",
        ],
        flashcards: [
          { front: "What is the shebang and what does it do?", back: "#!/bin/bash — first line of script that tells the system which interpreter to use" },
          { front: "How do you access the first argument passed to a script?", back: "$1" },
          { front: "What does $? contain?", back: "The exit status of the last command (0 = success)" },
          { front: "How do you check if a file exists in bash?", back: "if [ -f filename ]; then ... fi" },
          { front: "What does 'set -e' do in a script?", back: "Causes the script to exit immediately if any command returns a non-zero status" },
          { front: "How do you store command output in a variable?", back: "VAR=$(command) — e.g., TODAY=$(date +%Y-%m-%d)" },
        ],
      },
    ],
  },
  {
    key: "logging-troubleshooting",
    name: "Logging & Troubleshooting",
    level: "intermediate",
    topics: [
      {
        slug: "log-management",
        title: "Log Management & Troubleshooting",
        content: `Logs are essential for diagnosing problems. Linux stores logs in /var/log/ and through the systemd journal.\n\n**Key log files:**\n- /var/log/syslog (Debian) or /var/log/messages (RHEL) — general system log\n- /var/log/auth.log (Debian) or /var/log/secure (RHEL) — authentication events\n- /var/log/kern.log — kernel messages\n- /var/log/dmesg — boot/hardware messages\n- /var/log/apache2/ or /var/log/httpd/ — web server logs\n- /var/log/nginx/ — Nginx logs\n- /var/log/apt/ — APT package manager logs\n\n**journalctl (systemd journal):**\n- \`journalctl\` — all logs\n- \`journalctl -u sshd\` — logs for SSH service\n- \`journalctl -f\` — follow (real-time)\n- \`journalctl --since "2024-01-01" --until "2024-01-02"\`\n- \`journalctl -p err\` — only errors and worse\n- \`journalctl -b -1\` — logs from previous boot\n- \`journalctl _UID=1000\` — logs from specific user\n\n**Log rotation:**\n- logrotate handles automatic log file rotation\n- Config: /etc/logrotate.conf and /etc/logrotate.d/\n- Prevents logs from filling up disk\n\n**dmesg** — kernel ring buffer:\n- \`dmesg\` — all kernel messages\n- \`dmesg -T\` — with human-readable timestamps\n- \`dmesg | grep -i error\` — find hardware errors\n\n**Troubleshooting checklist:**\n1. Check service status: \`systemctl status service\`\n2. Check logs: \`journalctl -xe -u service\`\n3. Check disk space: \`df -h\`\n4. Check memory: \`free -h\`\n5. Check processes: \`top\` or \`ps aux\`\n6. Check network: \`ip a\`, \`ss -tulnp\`, \`ping\`\n7. Check DNS: \`dig domain\`\n8. Check permissions: \`ls -la\`, \`namei -l /path/to/file\``,
        commands: [
          { cmd: "journalctl -xe", desc: "Recent logs with extra detail" },
          { cmd: "journalctl -u nginx --since '1 hour ago'", desc: "Recent nginx logs" },
          { cmd: "tail -f /var/log/syslog", desc: "Follow system log" },
          { cmd: "dmesg -T | tail -20", desc: "Recent kernel messages" },
          { cmd: "sudo logrotate -f /etc/logrotate.conf", desc: "Force log rotation" },
        ],
        tips: [
          "Start troubleshooting with 'journalctl -xe' for quick error context",
          "Use 'journalctl --vacuum-size=500M' to reclaim journal disk space",
          "Check multiple log sources — not all services log to the journal",
          "Use 'last' and 'lastlog' to check user login history",
        ],
        flashcards: [
          { front: "Where is the main system log on Debian/Ubuntu?", back: "/var/log/syslog" },
          { front: "Where are authentication logs on Debian?", back: "/var/log/auth.log" },
          { front: "How do you view logs for a specific systemd service?", back: "journalctl -u servicename" },
          { front: "What does 'dmesg' show?", back: "Kernel ring buffer messages (hardware, drivers, boot messages)" },
          { front: "What tool manages automatic log rotation?", back: "logrotate (configured in /etc/logrotate.conf and /etc/logrotate.d/)" },
          { front: "How do you follow logs in real-time with journalctl?", back: "journalctl -f" },
        ],
      },
    ],
  },
  {
    key: "shell-environment",
    name: "Shell Environment & Configuration",
    level: "beginner",
    topics: [
      {
        slug: "shell-environment",
        title: "Shell Environment & Configuration",
        content: `The shell environment controls how your terminal behaves. Understanding environment variables and shell config files is essential.\n\n**Environment variables:**\n- \`echo $PATH\` — show current PATH\n- \`echo $HOME\` — home directory\n- \`echo $USER\` — current username\n- \`echo $SHELL\` — current shell\n- \`echo $HOSTNAME\` — machine name\n- \`env\` — show all environment variables\n- \`printenv\` — same as env\n- \`set\` — show all variables (including shell-local)\n\n**Setting variables:**\n- \`VAR=value\` — set shell variable (local to current shell)\n- \`export VAR=value\` — export as environment variable (available to child processes)\n- \`unset VAR\` — remove a variable\n- \`export PATH=$PATH:/new/path\` — append to PATH\n\n**Shell startup files (bash):**\nLogin shell reads (in order):\n1. /etc/profile (system-wide)\n2. ~/.bash_profile or ~/.bash_login or ~/.profile (user)\n\nNon-login interactive shell reads:\n1. /etc/bash.bashrc (system-wide)\n2. ~/.bashrc (user)\n\n**Common config tasks in ~/.bashrc:**\n- \`alias ll='ls -la'\` — create shortcuts\n- \`alias grep='grep --color=auto'\`\n- \`export EDITOR=vim\` — default editor\n- \`export PATH=$PATH:$HOME/bin\` — add custom bin to PATH\n- After editing: \`source ~/.bashrc\` to reload\n\n**Other useful commands:**\n- \`which command\` — find command location\n- \`type command\` — show how command is interpreted\n- \`alias\` — list all aliases\n- \`history\` — show command history\n- \`!!\` — repeat last command\n- \`!n\` — repeat command number n from history`,
        commands: [
          { cmd: "echo $PATH", desc: "Display PATH variable" },
          { cmd: "export VAR=value", desc: "Set and export environment variable" },
          { cmd: "source ~/.bashrc", desc: "Reload shell configuration" },
          { cmd: "alias ll='ls -la'", desc: "Create an alias" },
          { cmd: "env | grep LANG", desc: "Search environment variables" },
          { cmd: "history | tail -20", desc: "Show last 20 commands" },
        ],
        tips: [
          "Put aliases and exports in ~/.bashrc for persistence across sessions",
          "Use 'source' or '.' to reload config: source ~/.bashrc or . ~/.bashrc",
          "Login shell vs non-login shell determines which files are read",
          "Use 'type command' to check if something is an alias, function, or binary",
        ],
        flashcards: [
          { front: "What is the difference between a shell variable and an environment variable?", back: "Shell variables are local to the current shell. Environment variables (set with export) are inherited by child processes." },
          { front: "Which file should you edit for persistent bash aliases?", back: "~/.bashrc (for interactive non-login shells, which is the most common)" },
          { front: "How do you add a directory to PATH?", back: "export PATH=$PATH:/new/directory" },
          { front: "How do you reload ~/.bashrc without logging out?", back: "source ~/.bashrc (or . ~/.bashrc)" },
          { front: "What does the 'export' command do?", back: "Makes a variable available to child processes (promotes shell var to environment var)" },
          { front: "What is the difference between ~/.bash_profile and ~/.bashrc?", back: "~/.bash_profile is read by login shells, ~/.bashrc by interactive non-login shells" },
        ],
      },
    ],
  },
  {
    key: "find-locate",
    name: "Find & Locate",
    level: "beginner",
    topics: [
      {
        slug: "find-locate",
        title: "Find & Locate",
        content: `Finding files is one of the most common sysadmin tasks. The \`find\` command is extremely powerful and versatile.\n\n**find** — search for files in a directory hierarchy:\n\n**By name:**\n- \`find /path -name "*.log"\` — find by name (case-sensitive)\n- \`find /path -iname "*.LOG"\` — case-insensitive\n- \`find / -name "nginx.conf"\` — find a specific file\n\n**By type:**\n- \`find /path -type f\` — files only\n- \`find /path -type d\` — directories only\n- \`find /path -type l\` — symlinks only\n\n**By size:**\n- \`find /path -size +100M\` — larger than 100MB\n- \`find /path -size -1k\` — smaller than 1KB\n- \`find /path -empty\` — empty files/directories\n\n**By time:**\n- \`find /path -mtime -7\` — modified in last 7 days\n- \`find /path -mtime +30\` — modified more than 30 days ago\n- \`find /path -mmin -60\` — modified in last 60 minutes\n- \`find /path -newer reference.txt\` — newer than reference file\n\n**By permissions/owner:**\n- \`find /path -perm 644\` — exact permissions\n- \`find /path -perm -u+w\` — at least user write\n- \`find /path -user alice\` — owned by alice\n- \`find /path -group devs\` — owned by group devs\n\n**Actions:**\n- \`find /path -name "*.tmp" -delete\` — delete matches\n- \`find /path -name "*.sh" -exec chmod +x {} \\;\` — execute command on each\n- \`find /path -name "*.log" -exec grep "error" {} +\` — grep in found files\n- \`find /path -type f -print0 | xargs -0 rm\` — safe delete with xargs\n\n**locate** — fast file search using a database:\n- \`locate filename\` — instant search (uses pre-built index)\n- \`sudo updatedb\` — update the locate database\n- \`locate -i filename\` — case-insensitive\n- Much faster than find but may have stale results`,
        commands: [
          { cmd: "find / -name '*.conf' -type f", desc: "Find all .conf files" },
          { cmd: "find /var/log -size +50M", desc: "Find large log files" },
          { cmd: "find /home -mtime -7 -type f", desc: "Files modified in last week" },
          { cmd: "find . -name '*.tmp' -delete", desc: "Delete all .tmp files" },
          { cmd: "find /path -exec grep 'pattern' {} +", desc: "Search within found files" },
          { cmd: "locate nginx.conf", desc: "Quickly find file by name" },
        ],
        tips: [
          "find searches in real-time (slow but accurate); locate uses a database (fast but may be stale)",
          "Use -print0 with xargs -0 for filenames containing spaces",
          "Combine conditions: find /path -name '*.log' -size +10M -mtime +30",
          "Use {} + instead of {} \\; with -exec for better performance (batches arguments)",
        ],
        flashcards: [
          { front: "How do you find files larger than 100MB?", back: "find /path -size +100M" },
          { front: "How do you find files modified in the last 7 days?", back: "find /path -mtime -7" },
          { front: "What does -exec {} \\; do with find?", back: "Executes a command on each found file, {} is replaced with the filename" },
          { front: "What is the difference between find and locate?", back: "find searches in real-time (accurate but slow), locate uses a pre-built database (fast but may be outdated)" },
          { front: "How do you update the locate database?", back: "sudo updatedb" },
          { front: "How do you find empty files and directories?", back: "find /path -empty" },
        ],
      },
    ],
  },
  {
    key: "links-inodes",
    name: "Links & Inodes",
    level: "beginner",
    topics: [
      {
        slug: "links",
        title: "Hard Links & Symbolic Links",
        content: `Links create references to files. Understanding inodes is key to understanding links.\n\n**Inodes:**\nEvery file on a Linux filesystem has an inode — a data structure storing file metadata (permissions, owner, timestamps, data block pointers). The filename is just a pointer to the inode.\n- \`ls -i file\` — show inode number\n- \`stat file\` — detailed inode info\n\n**Hard links:**\n- \`ln original.txt hardlink.txt\` — create hard link\n- Both names point to the same inode (same data)\n- Deleting the original does NOT delete the data (still accessible via hard link)\n- Cannot cross filesystem boundaries\n- Cannot link to directories (usually)\n- \`ls -l\` shows link count (2nd column)\n\n**Symbolic (soft) links:**\n- \`ln -s /path/to/original symlink\` — create symlink\n- Points to the filename (path), not the inode\n- Can cross filesystem boundaries\n- Can link to directories\n- If original is deleted, symlink becomes a "dangling" (broken) link\n- \`ls -l\` shows \`l\` type and \`->\` pointing to target\n\n**Key differences:**\n| Feature | Hard Link | Symbolic Link |\n|---------|-----------|---------------|\n| Points to | Inode | Filename/path |\n| Cross filesystems | No | Yes |\n| Link to directories | No | Yes |\n| Original deleted | Data still accessible | Broken link |\n| Inode number | Same as original | Different |\n\n**Common uses of symlinks:**\n- \`/usr/bin/python -> /usr/bin/python3\` — version management\n- Linking config files to shared locations\n- Creating shortcuts to deeply nested paths`,
        commands: [
          { cmd: "ln original.txt hardlink.txt", desc: "Create hard link" },
          { cmd: "ln -s /path/to/file symlink", desc: "Create symbolic link" },
          { cmd: "ls -li file", desc: "Show inode number and details" },
          { cmd: "stat file", desc: "Display inode metadata" },
          { cmd: "readlink symlink", desc: "Show target of a symlink" },
          { cmd: "find /path -type l", desc: "Find all symbolic links" },
        ],
        tips: [
          "Use absolute paths when creating symlinks to avoid broken links if you move the symlink",
          "Hard links share the same inode — changes to either file affect both",
          "Use 'readlink -f symlink' to resolve the final target of chained symlinks",
          "Check for broken symlinks: find /path -xtype l",
        ],
        flashcards: [
          { front: "What is an inode?", back: "A data structure storing file metadata (permissions, owner, timestamps, pointers to data blocks). Each file has a unique inode number." },
          { front: "What is the difference between hard and symbolic links?", back: "Hard links point to the same inode (data). Symlinks point to a filename/path. Hard links survive original deletion, symlinks don't." },
          { front: "Can hard links cross filesystem boundaries?", back: "No. Hard links must be on the same filesystem. Symlinks can cross filesystems." },
          { front: "What happens to a symlink if the original file is deleted?", back: "It becomes a dangling (broken) link — pointing to a path that no longer exists." },
          { front: "How do you create a symbolic link?", back: "ln -s /path/to/target linkname" },
          { front: "Can you create a hard link to a directory?", back: "No (with rare exceptions). Only symlinks can link to directories." },
        ],
      },
    ],
  },
  {
    key: "archiving-compression",
    name: "Archiving & Compression",
    level: "beginner",
    topics: [
      {
        slug: "archiving",
        title: "Archiving & Compression",
        content: `Archiving bundles files together; compression reduces their size. Both are essential for backups, transfers, and package management.\n\n**tar (Tape Archive)** — the primary archiving tool:\n\n**Creating archives:**\n- \`tar cf archive.tar files/\` — create archive (no compression)\n- \`tar czf archive.tar.gz files/\` — create with gzip compression\n- \`tar cjf archive.tar.bz2 files/\` — create with bzip2 compression\n- \`tar cJf archive.tar.xz files/\` — create with xz compression (best ratio)\n\n**Extracting:**\n- \`tar xf archive.tar\` — extract (auto-detects compression)\n- \`tar xf archive.tar.gz\` — extract gzip archive\n- \`tar xf archive.tar -C /destination/\` — extract to specific directory\n\n**Listing contents:**\n- \`tar tf archive.tar\` — list files without extracting\n- \`tar tvf archive.tar\` — verbose list with details\n\n**tar flags:** c=create, x=extract, t=list, f=file, z=gzip, j=bzip2, J=xz, v=verbose\n\n**Individual compression tools:**\n- \`gzip file\` — compress (replaces file with file.gz)\n- \`gunzip file.gz\` — decompress\n- \`bzip2 file\` — better compression, slower\n- \`bunzip2 file.bz2\` — decompress\n- \`xz file\` — best compression, slowest\n- \`unxz file.xz\` — decompress\n\n**zip/unzip (cross-platform):**\n- \`zip -r archive.zip directory/\` — create zip\n- \`unzip archive.zip\` — extract\n- \`unzip -l archive.zip\` — list contents\n\n**Compression comparison:**\n| Tool | Speed | Compression | Extension |\n|------|-------|------------|----------|\n| gzip | Fast | Good | .gz |\n| bzip2 | Medium | Better | .bz2 |\n| xz | Slow | Best | .xz |\n| zip | Fast | Good | .zip |`,
        commands: [
          { cmd: "tar czf backup.tar.gz /data/", desc: "Create compressed archive" },
          { cmd: "tar xf archive.tar.gz -C /dest/", desc: "Extract to directory" },
          { cmd: "tar tf archive.tar.gz", desc: "List archive contents" },
          { cmd: "zip -r project.zip project/", desc: "Create zip archive" },
          { cmd: "gzip largefile.log", desc: "Compress a file with gzip" },
          { cmd: "gunzip file.gz", desc: "Decompress gzip file" },
        ],
        tips: [
          "tar xf auto-detects compression — no need to specify z/j/J when extracting",
          "Use xz for archives you store long-term (best compression); gzip for speed",
          "Always use 'tar tf' to inspect an archive before extracting",
          "Add -v for verbose output to see what's being archived/extracted",
        ],
        flashcards: [
          { front: "How do you create a gzip-compressed tar archive?", back: "tar czf archive.tar.gz files/" },
          { front: "How do you extract a tar archive to a specific directory?", back: "tar xf archive.tar -C /destination/" },
          { front: "What do the tar flags c, x, t, f stand for?", back: "c=create, x=extract, t=list, f=file" },
          { front: "Which compression gives the best ratio: gzip, bzip2, or xz?", back: "xz gives the best compression ratio but is the slowest" },
          { front: "How do you list contents of a tar archive without extracting?", back: "tar tf archive.tar" },
          { front: "How do you create a zip archive of a directory?", back: "zip -r archive.zip directory/" },
        ],
      },
    ],
  },
  {
    key: "hardware-info",
    name: "Hardware & System Info",
    level: "intermediate",
    topics: [
      {
        slug: "hardware-system-info",
        title: "Hardware & System Information",
        content: `Knowing how to inspect system hardware and configuration is essential for troubleshooting and capacity planning.\n\n**System overview:**\n- \`uname -a\` — kernel version, hostname, architecture\n- \`hostnamectl\` — hostname, OS, kernel, architecture\n- \`cat /etc/os-release\` — distribution info\n- \`uptime\` — how long system has been running, load averages\n- \`arch\` — CPU architecture (x86_64, aarch64)\n\n**CPU information:**\n- \`lscpu\` — detailed CPU info (cores, threads, speed, cache)\n- \`cat /proc/cpuinfo\` — raw CPU details\n- \`nproc\` — number of available CPUs\n\n**Memory:**\n- \`free -h\` — RAM and swap usage\n- \`cat /proc/meminfo\` — detailed memory info\n- \`vmstat\` — virtual memory statistics\n\n**Hardware devices:**\n- \`lspci\` — list PCI devices (GPUs, network cards, etc.)\n- \`lsusb\` — list USB devices\n- \`lsblk\` — list block devices (disks)\n- \`lshw\` — comprehensive hardware listing (may need sudo)\n- \`dmidecode\` — BIOS/firmware/hardware info (needs sudo)\n\n**System load:**\n- \`uptime\` — load averages (1, 5, 15 minutes)\n- Load of 1.0 = one CPU fully utilized\n- Load > number of CPUs = system is overloaded\n- \`w\` — who is logged in + load average\n\n**Kernel info:**\n- \`uname -r\` — kernel version only\n- \`cat /proc/version\` — kernel version with compiler info\n- \`dmesg\` — kernel ring buffer messages`,
        commands: [
          { cmd: "uname -a", desc: "Show all system info" },
          { cmd: "lscpu", desc: "CPU details" },
          { cmd: "free -h", desc: "Memory usage" },
          { cmd: "lspci", desc: "List PCI devices" },
          { cmd: "lsusb", desc: "List USB devices" },
          { cmd: "cat /etc/os-release", desc: "Distribution info" },
          { cmd: "uptime", desc: "System uptime and load" },
        ],
        tips: [
          "Load average = average number of processes waiting for CPU. Compare to nproc output.",
          "Use 'lshw -short' for a concise hardware summary",
          "dmidecode is great for finding server model, serial number, RAM slot details",
          "/proc is a virtual filesystem — reading files here doesn't access disk",
        ],
        flashcards: [
          { front: "How do you check the kernel version?", back: "uname -r (or uname -a for full details)" },
          { front: "How do you check CPU info?", back: "lscpu or cat /proc/cpuinfo" },
          { front: "What does 'free -h' show?", back: "RAM and swap usage in human-readable format" },
          { front: "How do you check which Linux distribution you're running?", back: "cat /etc/os-release" },
          { front: "What are load averages?", back: "Average number of processes waiting for CPU over 1, 5, and 15 minutes" },
          { front: "How do you list PCI devices (GPUs, NICs)?", back: "lspci" },
        ],
      },
    ],
  },
  {
    key: "boot-process",
    name: "Boot Process & GRUB",
    level: "intermediate",
    topics: [
      {
        slug: "boot-grub",
        title: "Boot Process & GRUB",
        content: `Understanding the Linux boot process is critical for troubleshooting boot failures.\n\n**Boot sequence:**\n1. **BIOS/UEFI** — firmware initializes hardware, runs POST\n2. **Bootloader (GRUB2)** — presents boot menu, loads kernel\n3. **Kernel** — initializes hardware, mounts root filesystem\n4. **Init system (systemd)** — starts services, reaches target\n\n**GRUB2 (GRand Unified Bootloader):**\n- Config file: /etc/default/grub (user settings)\n- Generated config: /boot/grub/grub.cfg (DO NOT edit directly)\n- \`sudo update-grub\` (Debian) or \`grub2-mkconfig -o /boot/grub2/grub.cfg\` (RHEL)\n\n**/etc/default/grub settings:**\n- GRUB_TIMEOUT=5 — seconds to wait at boot menu\n- GRUB_DEFAULT=0 — default boot entry\n- GRUB_CMDLINE_LINUX="" — kernel parameters\n- After changes: run update-grub to regenerate config\n\n**Kernel parameters:**\n- \`quiet\` — suppress boot messages\n- \`splash\` — show splash screen\n- \`single\` or \`1\` — boot to single-user/rescue mode\n- \`init=/bin/bash\` — emergency: boot straight to shell\n- View current: \`cat /proc/cmdline\`\n\n**Rescue & Recovery:**\n- At GRUB menu, press 'e' to edit boot entry\n- Add \`single\` or \`systemd.unit=rescue.target\` to kernel line\n- Press Ctrl+X to boot with changes\n- Emergency target: \`systemd.unit=emergency.target\`\n\n**systemd targets (runlevels):**\n- poweroff.target (0) — halt\n- rescue.target (1) — single-user\n- multi-user.target (3) — CLI multi-user\n- graphical.target (5) — GUI\n- reboot.target (6) — reboot\n- \`systemctl get-default\` — show current default\n- \`systemctl set-default multi-user.target\` — set to CLI boot`,
        commands: [
          { cmd: "cat /proc/cmdline", desc: "Show current kernel boot parameters" },
          { cmd: "sudo update-grub", desc: "Regenerate GRUB config (Debian)" },
          { cmd: "systemctl get-default", desc: "Show default boot target" },
          { cmd: "systemctl set-default graphical.target", desc: "Set GUI boot" },
          { cmd: "sudo grub2-mkconfig -o /boot/grub2/grub.cfg", desc: "Regenerate GRUB (RHEL)" },
        ],
        tips: [
          "Never edit /boot/grub/grub.cfg directly — edit /etc/default/grub and run update-grub",
          "At GRUB menu press 'e' to edit, Ctrl+X to boot — useful for password recovery",
          "If system won't boot, try adding 'init=/bin/bash' to kernel parameters for emergency access",
          "Use 'systemd-analyze blame' to see which services slow down boot",
        ],
        flashcards: [
          { front: "What is the Linux boot order?", back: "BIOS/UEFI → GRUB2 (bootloader) → Kernel → systemd (init)" },
          { front: "Where do you configure GRUB settings?", back: "/etc/default/grub (then run update-grub to apply)" },
          { front: "How do you boot into rescue/single-user mode?", back: "At GRUB menu press 'e', add 'single' or 'systemd.unit=rescue.target' to kernel line, Ctrl+X" },
          { front: "What does 'systemctl get-default' show?", back: "The default systemd target (runlevel) the system boots into" },
          { front: "How do you view current kernel boot parameters?", back: "cat /proc/cmdline" },
          { front: "What is the difference between rescue.target and emergency.target?", back: "rescue mounts filesystems and starts basic services; emergency only mounts root as read-only" },
        ],
      },
    ],
  },
  {
    key: "selinux-apparmor",
    name: "SELinux & AppArmor",
    level: "advanced",
    topics: [
      {
        slug: "selinux-apparmor",
        title: "SELinux & AppArmor",
        content: `Mandatory Access Controls (MAC) add security beyond standard file permissions. SELinux is used on RHEL/CentOS/Fedora; AppArmor on Ubuntu/SUSE.\n\n**SELinux (Security-Enhanced Linux):**\n\n**Modes:**\n- **Enforcing** — policies are enforced (blocks violations)\n- **Permissive** — logs violations but doesn't block\n- **Disabled** — SELinux is off\n\n**Checking & changing mode:**\n- \`getenforce\` — show current mode\n- \`sudo setenforce 0\` — switch to permissive (temporary)\n- \`sudo setenforce 1\` — switch to enforcing (temporary)\n- Permanent: edit /etc/selinux/config, set SELINUX=enforcing|permissive|disabled\n\n**SELinux contexts:**\nEvery file, process, and port has a context: user:role:type:level\n- \`ls -Z file\` — show file context\n- \`ps -eZ\` — show process contexts\n- \`id -Z\` — show current user context\n\n**Managing contexts:**\n- \`chcon -t httpd_sys_content_t /web/file\` — change context temporarily\n- \`restorecon -Rv /path\` — restore default contexts (preferred)\n- \`semanage fcontext -a -t httpd_sys_content_t "/web(/.*)?"\` — set permanent rule\n- Then: \`restorecon -Rv /web\`\n\n**SELinux booleans:**\n- \`getsebool -a\` — list all booleans\n- \`setsebool httpd_enable_homedirs on\` — temporary\n- \`setsebool -P httpd_enable_homedirs on\` — permanent\n\n**Troubleshooting:**\n- \`ausearch -m AVC -ts recent\` — recent denials\n- \`sealert -a /var/log/audit/audit.log\` — human-readable analysis\n- \`audit2allow\` — generate policy modules from denials\n\n**AppArmor (Ubuntu/SUSE):**\n- \`sudo aa-status\` — show AppArmor status\n- \`sudo aa-enforce /path/to/profile\` — enforce a profile\n- \`sudo aa-complain /path/to/profile\` — set to complain (log only)\n- Profiles stored in: /etc/apparmor.d/\n- \`sudo systemctl restart apparmor\` — apply changes`,
        commands: [
          { cmd: "getenforce", desc: "Check SELinux mode" },
          { cmd: "ls -Z /var/www/html/", desc: "Show file SELinux contexts" },
          { cmd: "sudo restorecon -Rv /path", desc: "Restore default SELinux contexts" },
          { cmd: "getsebool -a | grep httpd", desc: "Check HTTP-related booleans" },
          { cmd: "sudo aa-status", desc: "AppArmor status (Ubuntu)" },
          { cmd: "sudo ausearch -m AVC -ts recent", desc: "Recent SELinux denials" },
        ],
        tips: [
          "If a service isn't working after moving files, run 'restorecon -Rv' on the new path",
          "Never disable SELinux in production — use permissive mode for debugging",
          "Most SELinux issues are solved with restorecon or setting a boolean",
          "Use 'audit2why' to understand why something was denied",
        ],
        flashcards: [
          { front: "What are the three SELinux modes?", back: "Enforcing (blocks violations), Permissive (logs only), Disabled (off)" },
          { front: "How do you check the current SELinux mode?", back: "getenforce" },
          { front: "How do you restore default SELinux contexts?", back: "restorecon -Rv /path" },
          { front: "What is an SELinux boolean?", back: "A simple on/off switch that controls specific SELinux policy behaviors" },
          { front: "Where is the permanent SELinux mode configured?", back: "/etc/selinux/config (SELINUX=enforcing|permissive|disabled)" },
          { front: "What is AppArmor?", back: "A MAC system used by Ubuntu/SUSE (alternative to SELinux) that confines programs using profiles" },
        ],
      },
    ],
  },
  {
    key: "kernel-modules",
    name: "Kernel Modules & Parameters",
    level: "advanced",
    topics: [
      {
        slug: "kernel-modules",
        title: "Kernel Modules & Parameters",
        content: `The Linux kernel is modular — drivers and features can be loaded/unloaded at runtime as kernel modules.\n\n**Listing modules:**\n- \`lsmod\` — list loaded kernel modules\n- \`lsmod | grep module_name\` — check if specific module is loaded\n- \`modinfo module_name\` — show module information (author, description, parameters)\n\n**Loading and removing modules:**\n- \`sudo modprobe module_name\` — load module (handles dependencies)\n- \`sudo modprobe -r module_name\` — remove module\n- \`sudo insmod /path/to/module.ko\` — low-level load (no dependency handling)\n- \`sudo rmmod module_name\` — low-level remove\n\n**Persistent module configuration:**\n- /etc/modules-load.d/ — modules to load at boot\n- /etc/modprobe.d/ — module options and blacklisting\n- Blacklisting: create file in /etc/modprobe.d/blacklist-module.conf:\n  \`blacklist module_name\`\n\n**Kernel parameters (sysctl):**\n- \`sysctl -a\` — list all kernel parameters\n- \`sysctl net.ipv4.ip_forward\` — show specific parameter\n- \`sudo sysctl -w net.ipv4.ip_forward=1\` — set temporarily\n- Permanent: add to /etc/sysctl.conf or /etc/sysctl.d/*.conf\n- \`sudo sysctl -p\` — reload sysctl configuration\n\n**Common sysctl settings:**\n- \`net.ipv4.ip_forward=1\` — enable IP forwarding (routing)\n- \`vm.swappiness=10\` — reduce swap usage\n- \`fs.file-max=65535\` — max open files system-wide\n\n**/proc/sys/** mirrors sysctl values:\n- \`cat /proc/sys/net/ipv4/ip_forward\` — same as sysctl\n- \`echo 1 > /proc/sys/net/ipv4/ip_forward\` — temporary change`,
        commands: [
          { cmd: "lsmod", desc: "List loaded kernel modules" },
          { cmd: "modinfo e1000", desc: "Show info about a module" },
          { cmd: "sudo modprobe vfat", desc: "Load VFAT module" },
          { cmd: "sysctl -a | grep forward", desc: "Check IP forwarding" },
          { cmd: "sudo sysctl -w net.ipv4.ip_forward=1", desc: "Enable IP forwarding" },
          { cmd: "sudo sysctl -p", desc: "Reload sysctl settings" },
        ],
        tips: [
          "Use modprobe instead of insmod — it handles dependencies automatically",
          "Blacklist problematic modules in /etc/modprobe.d/ rather than just rmmod-ing them",
          "sysctl changes via -w are temporary — put permanent settings in /etc/sysctl.d/",
          "After editing sysctl files, run 'sysctl -p' or reboot to apply",
        ],
        flashcards: [
          { front: "How do you list loaded kernel modules?", back: "lsmod" },
          { front: "How do you load a kernel module?", back: "sudo modprobe module_name" },
          { front: "How do you blacklist a kernel module?", back: "Create /etc/modprobe.d/blacklist-name.conf with 'blacklist module_name'" },
          { front: "How do you set a kernel parameter permanently?", back: "Add it to /etc/sysctl.conf or /etc/sysctl.d/*.conf, then run sysctl -p" },
          { front: "What does modinfo show?", back: "Information about a kernel module: description, author, license, parameters" },
          { front: "How do you enable IP forwarding?", back: "sysctl -w net.ipv4.ip_forward=1 (temp) or add to /etc/sysctl.d/ (permanent)" },
        ],
      },
    ],
  },
  {
    key: "acls",
    name: "Access Control Lists (ACLs)",
    level: "advanced",
    topics: [
      {
        slug: "acls",
        title: "Access Control Lists",
        content: `ACLs extend basic Linux permissions beyond the owner/group/others model, allowing fine-grained access control for specific users and groups.\n\n**Why ACLs?**\nBasic permissions only allow one owner and one group. ACLs let you grant different permissions to multiple specific users and groups on the same file.\n\n**Viewing ACLs:**\n- \`getfacl file\` — display ACL entries\n- A \`+\` sign in \`ls -l\` output indicates an ACL is set: \`-rw-r--r--+\`\n\n**Setting ACLs:**\n- \`setfacl -m u:alice:rw file\` — give alice read/write\n- \`setfacl -m g:devs:rx file\` — give devs group read/execute\n- \`setfacl -m o::r file\` — set others to read\n- \`setfacl -x u:alice file\` — remove alice's ACL entry\n- \`setfacl -b file\` — remove all ACL entries\n\n**Default ACLs (for directories):**\nNew files created in the directory inherit these ACLs.\n- \`setfacl -d -m u:alice:rw dir/\` — set default ACL\n- \`setfacl -d -m g:devs:rx dir/\` — default group ACL\n- \`getfacl dir/\` — shows both access and default ACLs\n\n**Recursive ACLs:**\n- \`setfacl -R -m u:alice:rw dir/\` — apply recursively\n\n**ACL mask:**\nThe mask defines the maximum effective permissions for named users and groups.\n- \`setfacl -m m::rx file\` — set mask to read/execute\n- Effective permissions = ACL entry AND mask\n\n**Filesystem support:**\n- Filesystem must be mounted with ACL support\n- ext4 and XFS support ACLs by default\n- Check: \`mount | grep acl\` or \`tune2fs -l /dev/sda1 | grep acl\``,
        commands: [
          { cmd: "getfacl file", desc: "View ACL entries" },
          { cmd: "setfacl -m u:alice:rw file", desc: "Grant alice read/write" },
          { cmd: "setfacl -m g:devs:rx dir/", desc: "Grant devs group read/exec" },
          { cmd: "setfacl -d -m u:alice:rw dir/", desc: "Set default ACL on directory" },
          { cmd: "setfacl -b file", desc: "Remove all ACLs" },
          { cmd: "setfacl -R -m g:team:rw dir/", desc: "Recursive ACL" },
        ],
        tips: [
          "A + in ls -l output means the file has ACLs set",
          "Default ACLs on directories control permissions of newly created files within",
          "The ACL mask limits effective permissions — always check it with getfacl",
          "Use ACLs when you need more than one group to access the same files with different permissions",
        ],
        flashcards: [
          { front: "How do you view ACL entries on a file?", back: "getfacl filename" },
          { front: "How do you grant a specific user read/write via ACL?", back: "setfacl -m u:username:rw filename" },
          { front: "What is a default ACL?", back: "An ACL set on a directory that new files/directories inherit automatically" },
          { front: "How do you remove all ACLs from a file?", back: "setfacl -b filename" },
          { front: "How can you tell a file has ACLs from ls -l?", back: "A + sign appears after the permission string, e.g., -rw-r--r--+" },
          { front: "What is the ACL mask?", back: "Defines the maximum effective permissions for named users and groups (effective = entry AND mask)" },
        ],
      },
    ],
  },
  {
    key: "backup-recovery",
    name: "Backup & Recovery",
    level: "advanced",
    topics: [
      {
        slug: "backup-recovery",
        title: "Backup & Recovery",
        content: `A solid backup strategy is essential for any system administrator.\n\n**Backup types:**\n- **Full backup** — copies everything. Simplest to restore, most storage.\n- **Incremental** — copies only changes since last backup. Fastest, least storage.\n- **Differential** — copies changes since last full backup. Middle ground.\n\n**rsync** — the primary backup tool:\n- \`rsync -av /source/ /dest/\` — archive mode, verbose\n- \`rsync -avz /source/ user@remote:/dest/\` — sync to remote with compression\n- \`rsync -av --delete /source/ /dest/\` — mirror (delete files in dest not in source)\n- \`rsync -av --exclude='*.log' /source/ /dest/\` — exclude patterns\n- \`rsync -av --dry-run /source/ /dest/\` — preview without changes\n- Key: trailing slash on source matters! /source/ copies contents, /source copies the directory itself\n\n**tar for backups:**\n- \`tar czf /backup/full-$(date +%Y%m%d).tar.gz /data/\` — dated full backup\n- \`tar czf /backup/inc.tar.gz --newer="2024-01-01" /data/\` — incremental by date\n\n**dd** — low-level disk copy:\n- \`dd if=/dev/sda of=/backup/disk.img bs=4M status=progress\` — full disk image\n- \`dd if=/dev/sda1 of=/backup/part.img\` — partition image\n- \`dd if=/backup/disk.img of=/dev/sda\` — restore from image\n- WARNING: dd is dangerous — wrong of= can destroy data!\n\n**Backup best practices:**\n- **3-2-1 rule**: 3 copies, 2 different media types, 1 offsite\n- Test restores regularly\n- Automate with cron\n- Log and monitor backup jobs\n- Encrypt sensitive backups: \`tar czf - /data | gpg -c > backup.tar.gz.gpg\``,
        commands: [
          { cmd: "rsync -avz /data/ user@host:/backup/", desc: "Sync to remote server" },
          { cmd: "rsync -av --delete /src/ /dest/", desc: "Mirror source to dest" },
          { cmd: "rsync -av --dry-run /src/ /dest/", desc: "Preview rsync changes" },
          { cmd: "dd if=/dev/sda of=disk.img bs=4M status=progress", desc: "Full disk image" },
          { cmd: "tar czf backup-$(date +%F).tar.gz /data/", desc: "Dated backup archive" },
        ],
        tips: [
          "The trailing slash in rsync matters: /src/ copies contents, /src copies the directory",
          "Always use --dry-run first with rsync --delete to avoid accidental data loss",
          "dd has no confirmation — double-check if= and of= before pressing enter",
          "Schedule backups with cron and redirect output to a log file",
        ],
        flashcards: [
          { front: "What is the 3-2-1 backup rule?", back: "3 copies of data, on 2 different media types, with 1 copy offsite" },
          { front: "What is the difference between incremental and differential backups?", back: "Incremental: changes since last backup (any type). Differential: changes since last FULL backup." },
          { front: "How do you mirror a directory with rsync?", back: "rsync -av --delete /source/ /dest/ (--delete removes extra files in dest)" },
          { front: "What does 'dd' do?", back: "Low-level byte-for-byte copy of disks/partitions. Can create and restore disk images." },
          { front: "Why is the trailing slash important in rsync?", back: "/source/ copies contents into dest. /source (no slash) copies the directory itself into dest." },
          { front: "How do you preview rsync changes without executing?", back: "rsync -av --dry-run /source/ /dest/" },
        ],
      },
    ],
  },
  {
    key: "time-locale",
    name: "Time, Locale & NTP",
    level: "advanced",
    topics: [
      {
        slug: "time-locale-ntp",
        title: "Time, Locale & NTP",
        content: `Correct time synchronization and locale settings are critical for logs, certificates, and application behavior.\n\n**timedatectl** — manage system time:\n- \`timedatectl\` — show current time, timezone, NTP status\n- \`timedatectl set-timezone America/New_York\` — set timezone\n- \`timedatectl list-timezones\` — list all timezones\n- \`timedatectl set-time "2024-01-15 14:30:00"\` — set time manually\n- \`timedatectl set-ntp true\` — enable NTP sync\n\n**NTP (Network Time Protocol):**\nKeeps system clock synchronized with time servers.\n- **chrony** (modern, recommended):\n  - Config: /etc/chrony.conf or /etc/chrony/chrony.conf\n  - \`chronyc tracking\` — show sync status\n  - \`chronyc sources\` — show time sources\n  - \`systemctl status chronyd\` — service status\n- **systemd-timesyncd** (simpler, default on many distros):\n  - Config: /etc/systemd/timesyncd.conf\n  - \`timedatectl timesync-status\` — show sync info\n\n**date** command:\n- \`date\` — show current date/time\n- \`date +%Y-%m-%d\` — formatted date\n- \`date +%s\` — Unix timestamp (seconds since epoch)\n- \`date -d @1700000000\` — convert timestamp to human date\n\n**Timezone files:**\n- /etc/localtime — symlink to current timezone\n- /usr/share/zoneinfo/ — all timezone data files\n- \`ls -l /etc/localtime\` — see current timezone file\n\n**Locale settings:**\n- \`locale\` — show current locale settings\n- \`localectl\` — show and set system locale\n- \`localectl set-locale LANG=en_US.UTF-8\`\n- \`locale -a\` — list available locales\n- \`dpkg-reconfigure locales\` — configure locales (Debian)`,
        commands: [
          { cmd: "timedatectl", desc: "Show time and timezone info" },
          { cmd: "timedatectl set-timezone UTC", desc: "Set timezone to UTC" },
          { cmd: "chronyc tracking", desc: "NTP sync status" },
          { cmd: "date +%Y-%m-%d", desc: "Formatted current date" },
          { cmd: "locale", desc: "Show locale settings" },
          { cmd: "localectl set-locale LANG=en_US.UTF-8", desc: "Set system locale" },
        ],
        tips: [
          "Always use NTP in production — clock drift causes TLS/certificate failures",
          "Use 'timedatectl set-ntp true' to enable automatic time sync",
          "Set servers to UTC timezone to avoid daylight saving time confusion in logs",
          "chrony handles intermittent connectivity better than ntpd",
        ],
        flashcards: [
          { front: "How do you check the current timezone?", back: "timedatectl (shows timezone and NTP status)" },
          { front: "How do you change the timezone?", back: "timedatectl set-timezone Region/City (e.g., America/New_York)" },
          { front: "What is chrony used for?", back: "NTP time synchronization — keeps the system clock accurate" },
          { front: "How do you enable NTP time sync?", back: "timedatectl set-ntp true" },
          { front: "Where is the timezone data stored?", back: "/etc/localtime (symlink) pointing to a file in /usr/share/zoneinfo/" },
          { front: "How do you check the current locale?", back: "locale (or localectl)" },
        ],
      },
    ],
  },
  {
    key: "firewall-advanced",
    name: "Firewall In-Depth",
    level: "advanced",
    topics: [
      {
        slug: "firewall-advanced",
        title: "Firewall In-Depth (iptables, firewalld, nftables)",
        content: `Firewalls control network traffic. Linux offers multiple firewall tools.\n\n**iptables** — traditional packet filter:\n\n**Chains:**\n- **INPUT** — incoming traffic to this machine\n- **OUTPUT** — outgoing traffic from this machine\n- **FORWARD** — traffic passing through (routing)\n\n**Basic commands:**\n- \`sudo iptables -L -n -v\` — list all rules\n- \`sudo iptables -A INPUT -p tcp --dport 22 -j ACCEPT\` — allow SSH\n- \`sudo iptables -A INPUT -p tcp --dport 80 -j ACCEPT\` — allow HTTP\n- \`sudo iptables -A INPUT -j DROP\` — drop all other input\n- \`sudo iptables -D INPUT 3\` — delete rule #3\n- \`sudo iptables -F\` — flush (delete) all rules\n- \`sudo iptables -P INPUT DROP\` — set default policy to drop\n\n**Saving rules:**\n- \`sudo iptables-save > /etc/iptables/rules.v4\` — save rules\n- \`sudo iptables-restore < /etc/iptables/rules.v4\` — restore rules\n\n**firewalld** (RHEL/CentOS/Fedora):\nZone-based firewall management.\n- \`sudo firewall-cmd --state\` — check if running\n- \`sudo firewall-cmd --get-active-zones\` — active zones\n- \`sudo firewall-cmd --zone=public --add-service=http\` — allow HTTP (temp)\n- \`sudo firewall-cmd --zone=public --add-service=http --permanent\` — permanent\n- \`sudo firewall-cmd --zone=public --add-port=8080/tcp --permanent\`\n- \`sudo firewall-cmd --reload\` — apply permanent changes\n- \`sudo firewall-cmd --list-all\` — show current rules\n\n**nftables** — modern replacement for iptables:\n- \`sudo nft list ruleset\` — show all rules\n- \`sudo nft add rule inet filter input tcp dport 22 accept\`\n- Config: /etc/nftables.conf\n\n**UFW** (Ubuntu — simplified frontend):\n- \`sudo ufw enable/disable\` — toggle firewall\n- \`sudo ufw allow 22/tcp\` — allow SSH\n- \`sudo ufw deny 3306\` — block MySQL\n- \`sudo ufw status verbose\` — show rules`,
        commands: [
          { cmd: "sudo iptables -L -n -v", desc: "List all iptables rules" },
          { cmd: "sudo firewall-cmd --list-all", desc: "Show firewalld rules" },
          { cmd: "sudo ufw status verbose", desc: "Show UFW rules" },
          { cmd: "sudo nft list ruleset", desc: "Show nftables rules" },
          { cmd: "sudo iptables -A INPUT -p tcp --dport 443 -j ACCEPT", desc: "Allow HTTPS" },
          { cmd: "sudo firewall-cmd --add-service=https --permanent", desc: "Allow HTTPS (firewalld)" },
        ],
        tips: [
          "Always allow SSH (port 22) BEFORE setting default policy to DROP, or you'll lock yourself out",
          "firewalld uses zones — 'public' is the default zone for external interfaces",
          "nftables is the successor to iptables — new installations should prefer it",
          "Use --permanent with firewall-cmd, then --reload to apply",
        ],
        flashcards: [
          { front: "What are the three main iptables chains?", back: "INPUT (incoming), OUTPUT (outgoing), FORWARD (passing through)" },
          { front: "How do you save iptables rules permanently?", back: "iptables-save > /etc/iptables/rules.v4" },
          { front: "How do you allow HTTP in firewalld permanently?", back: "firewall-cmd --zone=public --add-service=http --permanent && firewall-cmd --reload" },
          { front: "What is the difference between iptables and nftables?", back: "nftables is the modern replacement with better syntax, performance, and atomic rule updates" },
          { front: "What is a firewalld zone?", back: "A trust level for network connections — rules are applied per-zone (e.g., public, internal, trusted)" },
          { front: "How do you list all firewall rules with UFW?", back: "sudo ufw status verbose" },
        ],
      },
    ],
  },
  {
    key: "raid",
    name: "RAID",
    level: "advanced",
    topics: [
      {
        slug: "raid",
        title: "RAID (Redundant Array of Independent Disks)",
        content: `RAID combines multiple disks for redundancy, performance, or both.\n\n**RAID levels:**\n\n**RAID 0 (Striping):**\n- Data split across disks — fast reads/writes\n- No redundancy — one disk failure = all data lost\n- Min disks: 2\n- Use case: temporary/scratch data needing speed\n\n**RAID 1 (Mirroring):**\n- Exact copy on each disk\n- Survives one disk failure\n- 50% storage efficiency\n- Min disks: 2\n- Use case: OS drive, critical data\n\n**RAID 5 (Striping with parity):**\n- Data + parity distributed across disks\n- Survives one disk failure\n- Good balance of speed, capacity, redundancy\n- Min disks: 3\n- Use case: general purpose servers\n\n**RAID 6 (Double parity):**\n- Survives two disk failures\n- Min disks: 4\n- Slower writes than RAID 5\n\n**RAID 10 (1+0, Mirror + Stripe):**\n- Mirrored pairs that are striped\n- Best performance + redundancy\n- 50% storage efficiency\n- Min disks: 4\n- Use case: databases, high-performance workloads\n\n**Software RAID with mdadm:**\n- \`sudo mdadm --create /dev/md0 --level=1 --raid-devices=2 /dev/sdb /dev/sdc\` — create RAID 1\n- \`cat /proc/mdstat\` — RAID status\n- \`sudo mdadm --detail /dev/md0\` — detailed info\n- \`sudo mdadm --manage /dev/md0 --add /dev/sdd\` — add spare disk\n- \`sudo mdadm --manage /dev/md0 --remove /dev/sdb\` — remove failed disk\n- Save config: \`sudo mdadm --detail --scan >> /etc/mdadm/mdadm.conf\`\n\n**Hardware vs Software RAID:**\n- Hardware RAID: dedicated controller card, better performance, transparent to OS\n- Software RAID (mdadm): no special hardware, managed by OS, more flexible`,
        commands: [
          { cmd: "cat /proc/mdstat", desc: "Check RAID array status" },
          { cmd: "sudo mdadm --detail /dev/md0", desc: "Detailed RAID info" },
          { cmd: "sudo mdadm --create /dev/md0 --level=5 --raid-devices=3 /dev/sd[bcd]", desc: "Create RAID 5" },
          { cmd: "sudo mdadm --manage /dev/md0 --add /dev/sde", desc: "Add spare disk" },
        ],
        tips: [
          "RAID is NOT a backup — it protects against disk failure, not accidental deletion or corruption",
          "RAID 5 requires a rebuild when a disk fails — performance drops significantly during rebuild",
          "RAID 10 is preferred over RAID 5 for databases due to better write performance",
          "Always have a hot spare configured for automatic rebuild",
        ],
        flashcards: [
          { front: "What is RAID 0?", back: "Striping — splits data across disks for speed. No redundancy. One failure = total data loss." },
          { front: "What is RAID 1?", back: "Mirroring — exact copy on two disks. Survives one disk failure. 50% storage efficiency." },
          { front: "What is RAID 5?", back: "Striping with distributed parity. Survives one disk failure. Minimum 3 disks." },
          { front: "What is RAID 10?", back: "Mirror + Stripe. Best performance and redundancy. Minimum 4 disks, 50% efficiency." },
          { front: "What tool manages software RAID on Linux?", back: "mdadm" },
          { front: "Is RAID a replacement for backups?", back: "No! RAID protects against disk failure, not accidental deletion, corruption, or ransomware." },
        ],
      },
    ],
  },
  {
    key: "nfs-samba",
    name: "Network File Systems",
    level: "advanced",
    topics: [
      {
        slug: "network-filesystems",
        title: "NFS & Samba (Network File Sharing)",
        content: `Network file systems allow sharing files between machines over a network.\n\n**NFS (Network File System)** — Linux-to-Linux file sharing:\n\n**Server setup:**\n1. Install: \`sudo apt install nfs-kernel-server\`\n2. Add exports to /etc/exports:\n   \`/shared 192.168.1.0/24(rw,sync,no_subtree_check)\`\n3. Apply: \`sudo exportfs -ra\`\n4. Start service: \`sudo systemctl enable --now nfs-server\`\n\n**Client mount:**\n- \`sudo mount -t nfs server:/shared /mnt/nfs\` — manual mount\n- In /etc/fstab: \`server:/shared /mnt/nfs nfs defaults 0 0\` — persistent mount\n- \`showmount -e server\` — show server's exports\n\n**NFS options:**\n- rw/ro — read-write/read-only\n- sync — write to disk before confirming\n- no_root_squash — allow root access (use carefully)\n- root_squash — map root to nobody (default, safer)\n\n**Samba (SMB/CIFS)** — Linux-to-Windows file sharing:\n\n**Server setup:**\n1. Install: \`sudo apt install samba\`\n2. Edit /etc/samba/smb.conf:\n   \`[share]\n   path = /shared\n   browseable = yes\n   read only = no\n   valid users = alice\`\n3. Create Samba user: \`sudo smbpasswd -a alice\`\n4. Restart: \`sudo systemctl restart smbd\`\n\n**Client mount (CIFS):**\n- \`sudo mount -t cifs //server/share /mnt/smb -o username=alice\`\n- In /etc/fstab: \`//server/share /mnt/smb cifs credentials=/etc/samba/creds 0 0\`\n\n**autofs** — automatic mounting on access:\n- Install: \`sudo apt install autofs\`\n- Edit /etc/auto.master: \`/mnt/auto /etc/auto.nfs\`\n- Edit /etc/auto.nfs: \`data -rw,sync server:/shared\`\n- Mounts when accessed, unmounts after idle`,
        commands: [
          { cmd: "sudo exportfs -ra", desc: "Apply NFS export changes" },
          { cmd: "showmount -e server", desc: "Show NFS exports" },
          { cmd: "sudo mount -t nfs server:/share /mnt/nfs", desc: "Mount NFS share" },
          { cmd: "sudo smbpasswd -a alice", desc: "Create Samba user" },
          { cmd: "sudo mount -t cifs //server/share /mnt -o user=alice", desc: "Mount Samba share" },
        ],
        tips: [
          "NFS is simpler for Linux-to-Linux; Samba is needed for Windows interop",
          "Use 'root_squash' in NFS exports to prevent remote root from having root access",
          "Store CIFS credentials in a file (chmod 600) instead of on the mount command",
          "autofs is great for laptop users — shares mount on demand and unmount when idle",
        ],
        flashcards: [
          { front: "What is NFS used for?", back: "Sharing files between Linux systems over a network" },
          { front: "Where do you configure NFS exports on the server?", back: "/etc/exports" },
          { front: "What is Samba used for?", back: "Sharing files between Linux and Windows using the SMB/CIFS protocol" },
          { front: "How do you apply changes to /etc/exports?", back: "sudo exportfs -ra" },
          { front: "What does 'root_squash' do in NFS?", back: "Maps remote root user to nobody/nogroup, preventing root access on the NFS share" },
          { front: "What is autofs?", back: "Automatically mounts network shares on access and unmounts them after idle timeout" },
        ],
      },
    ],
  },
  {
    key: "containers",
    name: "Containers (Docker & Podman)",
    level: "advanced",
    topics: [
      {
        slug: "containers",
        title: "Containers (Docker & Podman)",
        content: `Containers package applications with their dependencies for consistent deployment. They are lighter than VMs.\n\n**Key concepts:**\n- **Image** — read-only template (like a snapshot)\n- **Container** — running instance of an image\n- **Registry** — image repository (Docker Hub, etc.)\n- **Dockerfile** — instructions to build an image\n\n**Docker basics:**\n- \`docker run hello-world\` — run a test container\n- \`docker run -d -p 8080:80 nginx\` — run nginx in background, map port 8080 to 80\n- \`docker run -it ubuntu bash\` — interactive container with bash\n- \`docker ps\` — list running containers\n- \`docker ps -a\` — list all containers (including stopped)\n- \`docker stop container_id\` — stop a container\n- \`docker rm container_id\` — remove stopped container\n- \`docker images\` — list images\n- \`docker rmi image_id\` — remove image\n- \`docker pull nginx\` — download image\n\n**Docker volumes (persistent data):**\n- \`docker run -v /host/path:/container/path nginx\` — bind mount\n- \`docker volume create mydata\` — create named volume\n- \`docker run -v mydata:/data nginx\` — use named volume\n\n**Dockerfile example:**\n\`FROM ubuntu:22.04\nRUN apt update && apt install -y nginx\nCOPY index.html /var/www/html/\nEXPOSE 80\nCMD ["nginx", "-g", "daemon off;"]\`\n\n**Build and run:**\n- \`docker build -t myapp .\` — build image\n- \`docker run -d -p 80:80 myapp\` — run your image\n\n**Podman** — Docker alternative (rootless, daemonless):\n- Same command syntax: \`podman run\`, \`podman ps\`, etc.\n- No daemon required (each container is a child process)\n- Rootless by default (more secure)\n- \`alias docker=podman\` — drop-in replacement`,
        commands: [
          { cmd: "docker run -d -p 8080:80 nginx", desc: "Run nginx container" },
          { cmd: "docker ps -a", desc: "List all containers" },
          { cmd: "docker build -t myapp .", desc: "Build image from Dockerfile" },
          { cmd: "docker exec -it container_id bash", desc: "Shell into running container" },
          { cmd: "docker logs container_id", desc: "View container logs" },
          { cmd: "docker volume ls", desc: "List Docker volumes" },
        ],
        tips: [
          "Use specific image tags (nginx:1.25) instead of 'latest' in production",
          "Podman is a drop-in Docker replacement that doesn't need a daemon running",
          "Use volumes for persistent data — container filesystem is ephemeral",
          "Clean up unused resources: docker system prune -a",
        ],
        flashcards: [
          { front: "What is the difference between a Docker image and a container?", back: "An image is a read-only template. A container is a running instance of an image." },
          { front: "How do you run a container in the background?", back: "docker run -d (detached mode)" },
          { front: "How do you map host port 8080 to container port 80?", back: "docker run -p 8080:80 image_name" },
          { front: "What is a Dockerfile?", back: "A text file with instructions to build a Docker image (FROM, RUN, COPY, CMD, etc.)" },
          { front: "What is Podman?", back: "A Docker-compatible container tool that is rootless and daemonless" },
          { front: "How do you persist data in Docker?", back: "Use volumes: docker run -v /host/path:/container/path or named volumes" },
        ],
      },
    ],
  },
  {
    key: "service-config",
    name: "Service Configuration",
    level: "advanced",
    topics: [
      {
        slug: "service-config",
        title: "Web Server & Service Configuration",
        content: `Configuring common services is a key sysadmin skill.\n\n**Apache (httpd):**\n- Install: \`sudo apt install apache2\` (Debian) or \`sudo dnf install httpd\` (RHEL)\n- Main config: /etc/apache2/apache2.conf (Debian) or /etc/httpd/conf/httpd.conf (RHEL)\n- Sites: /etc/apache2/sites-available/ and sites-enabled/\n- \`sudo a2ensite mysite.conf\` — enable site\n- \`sudo a2dissite default\` — disable site\n- \`sudo a2enmod rewrite\` — enable module\n- Document root: /var/www/html/\n- \`sudo apachectl configtest\` — test config syntax\n\n**Nginx:**\n- Install: \`sudo apt install nginx\`\n- Main config: /etc/nginx/nginx.conf\n- Sites: /etc/nginx/sites-available/ and sites-enabled/\n- \`sudo nginx -t\` — test configuration\n- \`sudo systemctl reload nginx\` — apply changes\n\n**Basic Nginx server block:**\nserver {\n  listen 80;\n  server_name example.com;\n  root /var/www/example;\n  index index.html;\n  location / { try_files $uri $uri/ =404; }\n}\n\n**DNS concepts:**\n- /etc/hosts — local name resolution\n- /etc/resolv.conf — DNS server configuration\n- /etc/nsswitch.conf — name resolution order\n- A record — hostname to IP\n- CNAME — alias to another hostname\n- MX — mail server\n- PTR — reverse DNS (IP to hostname)\n\n**DHCP basics:**\n- Server assigns IP addresses automatically\n- Config: /etc/dhcp/dhcpd.conf\n- Lease file: /var/lib/dhcp/dhcpd.leases\n- Client: \`dhclient eth0\` — request IP address`,
        commands: [
          { cmd: "sudo nginx -t", desc: "Test Nginx configuration" },
          { cmd: "sudo apachectl configtest", desc: "Test Apache configuration" },
          { cmd: "sudo a2ensite mysite.conf", desc: "Enable Apache site" },
          { cmd: "sudo a2enmod ssl", desc: "Enable Apache SSL module" },
          { cmd: "sudo systemctl reload nginx", desc: "Reload Nginx config" },
          { cmd: "cat /etc/hosts", desc: "View local DNS entries" },
        ],
        tips: [
          "Always test config before reloading: nginx -t or apachectl configtest",
          "Use 'reload' instead of 'restart' for zero-downtime config changes",
          "On Debian, use a2ensite/a2dissite to manage Apache virtual hosts",
          "/etc/hosts is checked before DNS — useful for overrides and testing",
        ],
        flashcards: [
          { front: "How do you test Nginx configuration?", back: "sudo nginx -t" },
          { front: "Where are Nginx site configs on Debian?", back: "/etc/nginx/sites-available/ (enabled via symlink in sites-enabled/)" },
          { front: "What does 'a2ensite' do?", back: "Enables an Apache virtual host by creating a symlink from sites-available to sites-enabled" },
          { front: "What is the difference between 'reload' and 'restart' for a web server?", back: "reload re-reads config with zero downtime; restart fully stops and starts the process" },
          { front: "What file provides local hostname-to-IP mapping?", back: "/etc/hosts" },
          { front: "What DNS record type maps a hostname to an IP address?", back: "A record (AAAA for IPv6)" },
        ],
      },
    ],
  },
  {
    key: "resource-limits",
    name: "System Resource Limits",
    level: "advanced",
    topics: [
      {
        slug: "resource-limits",
        title: "System Resource Limits & cgroups",
        content: `Resource limits prevent individual users or processes from consuming all system resources.\n\n**ulimit** — per-user resource limits:\n- \`ulimit -a\` — show all current limits\n- \`ulimit -n\` — max open files\n- \`ulimit -u\` — max user processes\n- \`ulimit -n 65535\` — set max open files (session only)\n\n**Permanent limits** — /etc/security/limits.conf:\nFormat: \`domain type resource value\`\n- \`alice hard nofile 65535\` — hard limit for alice\n- \`@devs soft nproc 4096\` — soft limit for devs group\n- \`* soft nofile 4096\` — all users\n\n**Types:**\n- **soft** — default limit (user can raise up to hard)\n- **hard** — maximum limit (only root can raise)\n\n**/etc/security/limits.d/** — drop-in config files\n\n**cgroups (Control Groups):**\nKernel feature to limit, prioritize, and account for resource usage by process groups.\n\n**cgroups v2 (modern):**\n- /sys/fs/cgroup/ — cgroup filesystem\n- Controllers: cpu, memory, io, pids\n- Used by systemd, Docker, Kubernetes\n\n**systemd resource controls:**\n- \`systemctl set-property httpd.service MemoryMax=512M\` — limit memory\n- \`systemctl set-property httpd.service CPUQuota=50%\` — limit CPU\n- In unit file [Service] section:\n  MemoryMax=512M\n  CPUQuota=50%\n  TasksMax=100\n\n**Viewing resource usage per service:**\n- \`systemd-cgtop\` — like top but for cgroups/services\n- \`systemctl show httpd.service -p MemoryMax\`\n\n**OOM (Out of Memory) Killer:**\n- Kernel kills processes when memory is exhausted\n- /proc/PID/oom_score — likelihood of being killed\n- /proc/PID/oom_score_adj — adjust (-1000 to 1000)`,
        commands: [
          { cmd: "ulimit -a", desc: "Show all resource limits" },
          { cmd: "ulimit -n 65535", desc: "Set max open files" },
          { cmd: "systemd-cgtop", desc: "Top-like view for cgroups" },
          { cmd: "systemctl set-property svc MemoryMax=512M", desc: "Limit service memory" },
        ],
        tips: [
          "Most 'too many open files' errors are fixed by increasing nofile in limits.conf",
          "Soft limits can be raised by users; hard limits require root",
          "systemd service limits override ulimit for services managed by systemd",
          "Use systemd-cgtop to see real-time resource usage per service",
        ],
        flashcards: [
          { front: "How do you check current resource limits?", back: "ulimit -a" },
          { front: "Where do you set permanent resource limits?", back: "/etc/security/limits.conf (or /etc/security/limits.d/)" },
          { front: "What is the difference between soft and hard limits?", back: "Soft = default (user can raise to hard limit). Hard = max (only root can raise)." },
          { front: "What are cgroups?", back: "Kernel feature to limit, prioritize, and account for CPU, memory, and I/O usage by process groups" },
          { front: "How do you limit a systemd service to 512MB RAM?", back: "systemctl set-property service.name MemoryMax=512M" },
          { front: "What does the OOM killer do?", back: "Kills processes when the system runs out of memory, choosing based on oom_score" },
        ],
      },
    ],
  },
  {
    key: "fhs",
    name: "Filesystem Hierarchy Standard",
    level: "beginner",
    topics: [
      {
        slug: "fhs",
        title: "Filesystem Hierarchy Standard (FHS)",
        content: `The FHS defines the directory structure on Linux systems. Knowing where things go is fundamental.\n\n**Root-level directories:**\n\n**/ (root)** — top of the filesystem tree\n\n**/bin** — essential user binaries (ls, cp, cat, bash)\n  - On modern systems often symlinked to /usr/bin\n\n**/sbin** — essential system binaries (fdisk, iptables, reboot)\n  - Often symlinked to /usr/sbin\n\n**/etc** — system configuration files\n  - /etc/passwd, /etc/fstab, /etc/nginx/, /etc/ssh/\n  - Almost everything you configure lives here\n\n**/home** — user home directories\n  - /home/alice, /home/bob\n\n**/root** — root user's home directory (not /home/root)\n\n**/var** — variable data (changes during operation)\n  - /var/log — log files\n  - /var/www — web server content\n  - /var/lib — application state data\n  - /var/tmp — temporary files (persist across reboots)\n  - /var/spool — queued data (mail, print)\n\n**/tmp** — temporary files (may be cleared on reboot)\n  - World-writable with sticky bit\n\n**/usr** — user programs and data (read-only in practice)\n  - /usr/bin — most user commands\n  - /usr/sbin — system admin commands\n  - /usr/lib — libraries\n  - /usr/share — architecture-independent data (docs, man pages)\n  - /usr/local — locally installed software (not from package manager)\n\n**/opt** — optional/third-party software\n  - /opt/google, /opt/myapp\n\n**/dev** — device files\n  - /dev/sda — first hard disk\n  - /dev/null — discard device\n  - /dev/zero — source of null bytes\n  - /dev/random — random data\n\n**/proc** — virtual filesystem (process/kernel info)\n  - /proc/cpuinfo, /proc/meminfo, /proc/PID/\n\n**/sys** — virtual filesystem (hardware/driver info)\n\n**/boot** — kernel and bootloader files\n  - vmlinuz (kernel), initrd/initramfs, grub/\n\n**/mnt** — temporary mount point\n**/media** — removable media mount points (USB, CD)`,
        commands: [
          { cmd: "ls /", desc: "List root directory contents" },
          { cmd: "du -sh /var/log", desc: "Check log directory size" },
          { cmd: "file /bin/ls", desc: "Identify binary type" },
          { cmd: "cat /proc/cpuinfo", desc: "CPU info from proc" },
        ],
        tips: [
          "Configuration goes in /etc, logs in /var/log, temp files in /tmp",
          "On modern distros /bin and /sbin are often symlinks to /usr/bin and /usr/sbin",
          "Use /usr/local for manually installed software to keep it separate from packages",
          "/opt is for third-party software that doesn't follow FHS structure",
        ],
        flashcards: [
          { front: "Where are system configuration files stored?", back: "/etc" },
          { front: "Where are log files stored?", back: "/var/log" },
          { front: "What is the difference between /tmp and /var/tmp?", back: "/tmp may be cleared on reboot; /var/tmp persists across reboots" },
          { front: "Where does locally installed software go?", back: "/usr/local (for software not installed via package manager)" },
          { front: "What is /proc?", back: "A virtual filesystem providing process and kernel information (not on disk)" },
          { front: "What is /dev/null?", back: "A special device file that discards all data written to it" },
          { front: "Where is the root user's home directory?", back: "/root (not /home/root)" },
          { front: "What is /opt used for?", back: "Optional/third-party software packages" },
        ],
      },
    ],
  },
];
